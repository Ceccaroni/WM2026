node_modules/
out/
dist/
dist-web/
.DS_Store
*.local
*.tsbuildinfo

# Generierter PWA-Seed (aus lokalem Mac-Store via `seed:web`); wird bei jedem
# build:web frisch erzeugt und nur in dist-web/ → gh-pages ausgeliefert.
src/web/seed-state.json

# Verirrtes Electron-Main-Bundle im Root (Build-Output gehört nach out/main/).
/index.js
