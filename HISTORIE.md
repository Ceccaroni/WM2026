# HISTORIE — Arbeitslog (append-only)

> Chronologisches Log aller Arbeitsblöcke, neueste oben — inkl. Verifikationsdetails, Quellen und Begründungen („warum ist X so gebaut?"). Wird beim Sessionstart NICHT gelesen; nur bei Detailfragen gezielt hier nachschlagen. Übergabe-Dokument: STATUS.md.

### PWA-Deploy live auf GitHub Pages (16.06.; Auslöser: Adrian „GitHub-Pages-Deploy")

**Ergebnis:** PWA ist öffentlich erreichbar unter **https://ceccaroni.github.io/WM2026/** (HTTPS erzwungen) — damit erstmals echter iPhone-Install + Service-Worker/Offline möglich (LAN-http hatte keinen SW).

- **Repo:** Arbeitsverzeichnis war kein Git-Repo → `git init -b main`, erster Commit (1323 Dateien). **Öffentliches** Repo `Ceccaroni/WM2026` (Adrian-Entscheid: Account ist Free-Plan → Pages nur für public Repos; der Seed ist über die Pages-URL ohnehin öffentlich, schon abgesegnet). Vor dem Push: Pfad-Hygiene + Secret-Scan (Tokens/Keys/PrivateKeys/Publish-Credentials) — sauber. Repo-Name = `WM2026`, damit Pages-URL-Subpfad zur eingebackenen `base '/WM2026/'` passt.
- **.gitignore erweitert:** zusätzlich `dist-web/` (50 MB Build-Output, fehlte!), `*.tsbuildinfo`, **`src/web/seed-state.json`** (generiertes Artefakt aus dem Mac-Store — gehört nicht in den Source-Branch, wird bei jedem `build:web` frisch erzeugt) und `/index.js` (verirrtes Electron-Main-Bundle im Root; korrekt ist `out/main/index.js`). `build/` (Icon-Assets, 2.5 MB) bleibt committet. `dist/` (772 MB .dmg) war schon ignoriert.
- **Deploy-Mechanik (NEU, wiederverwendbar):** `scripts/deploy-web.mjs` + Scripts `deploy:web` / `publish:web`. **Warum lokal statt CI:** der Web-Seed kommt aus dem führenden Mac-Store — ein Actions-Runner hat ihn NICHT, kann also nicht bauen. Daher: lokal `npm run build:web`, dann den fertigen `dist-web/` als **Orphan-Commit force-pushen in `gh-pages`** (Deploy-Artefakt-Branch, keine Historie nötig). Script setzt `.nojekyll` (sonst schluckt Jekyll evtl. Dateien), zieht die Remote-URL aus `origin`, räumt das temporäre `.git` in dist-web wieder weg. **Künftiges Tipp-Update online bringen: `npm run publish:web`** (kettet Build + Deploy; analog zur OTA-`publish:update` für die Electron-App).
- **Pages aktiviert** via API (`gh api … /pages`, source = gh-pages/root, `https_enforced:true`); erster Build „built" ohne Fehler.
- **Verifiziert (Artefakt-Ebene, real per HTTP):** Hauptseite 200 (`<title>WM26 Tipp`), die im HTML referenzierten `assets/index-*.js` + `*.css` 200, `manifest.webmanifest` (start_url/scope `.` → relativ, respektiert Subpfad), `sw.js` (precacht App-Shell), `icon-192/512.png` + `apple-touch-icon.png` (für iOS-Homescreen) je 200.
- 🔴 **NICHT verifiziert (braucht Adrian am iPhone):** Install zum Homescreen, Offline-Funktion (SW erst über https aktiv), Layout-Sichtung Hochformat aller Screens, Flip-Bug (Safari-3D). Reine Erreichbarkeit ≠ echter Nutzerfall.

### PWA fürs iPhone — Phase 1 gebaut (16.06.; Auslöser: Adrian „PWA jetzt bauen, aber sauber planen erst"; nach Verwurf der Bracket-Minimap)

**Ziel/Entscheidungen:** Renderer als eigenständige **statische PWA** (GitHub Pages, `base '/WM2026/'`), **kein Backend** (ESPN CORS-offen, in dieser Session am Endpunkt erneut bestätigt: Scoreboard+Summary `access-control-allow-origin: *`). News-Feeds + fixturedownload fallen im Web weg (kein CORS-Header). Plan: `~/.claude/plans/sorted-imagining-quill.md`.

- **Kernidee (max. Reuse, 0 Renderer-Umbau):** Der Renderer importiert nichts von Electron, spricht nur `window.wm26` (`Wm26Api`, 23 Methoden, 32 Aufrufstellen). Eine **Web-Bridge** implementiert dieselbe Oberfläche im Browser und setzt `window.wm26`, BEVOR React mountet → `src/renderer/**` praktisch unverändert.
- **Neue Web-Schicht (`src/web/`):** `bridge.ts` (window.wm26 für Browser), `store-web.ts` (Persistenz via IndexedDB statt fs; Logik gespiegelt von `src/main/store.ts`, inkl. „neueres exportedAt gewinnt"; **UUID-Fallback** via getRandomValues für LAN-http, da `crypto.randomUUID` nur im Secure Context), `espn-poll.ts` (ESPN-Polling im Browser; Parse 1:1 aus `src/shared/results.ts`; ohne fxd-Fallback/User-Agent-Header), `idb.ts` (KV-Store), `anpfiff.ts` (s.u.), `main.tsx` (Entry: Bridge → Anpfiff → dynamischer Import des Renderer-Bootstrap). Stubs bewusst: News/OTA-Update/Dock-Badge. Export/Import `.wm26tipp` über Blob-Download / File-Input. PDF → `window.print()`, ICS → Blob.
- **Build:** `vite.web.config.ts` (root `src/web`, `base` via `VITE_BASE`, `@data`-Alias, define inkl. `__APP_VERSION__`, `vite-plugin-pwa`). Scripts `build:web`/`preview:web`. PWA-Manifest + Service-Worker (`registerType:'autoUpdate'`, `injectRegister:false` → SW-Registrierung selbst in main.tsx, CSP-konform). **SW precacht nur App-Shell (~2.2 MB), NICHT die ~985 Spielerfotos/SVGs (~50 MB)** → Runtime-Cache CacheFirst. iOS-Meta-Tags + `connect-src` ESPN in CSP. `src/web` zu `tsconfig.web.json`-include. `typecheck` grün, Electron-`npm run build` grün (keine Regression).
- **Anpfiff-Screen (`anpfiff.ts`):** Browser/iOS verbieten Ton ohne User-Geste; das Auto-Intro hat keine. Lösung: Tschuttini-Logo → nach 2 s „Tippen zum Anpfiff" → Tap entsperrt Audio (spielt intro.mp3 stumm an) → dann Intro MIT Ton. Fonts früh geladen. **Von Adrian bestätigt: Ton läuft.**
- **Seed = ganze Tippgruppe (`scripts/build-web-seed.mjs` → `src/web/seed-state.json`):** liest den führenden Mac-Store (nur lesen) und backt alle 7 Profile (Adrian, Lisa, Leonor, Benjamin, Franzipani, Claude, Martin; je 104 Tipps, Claude 101) als Startzustand ein. `store-web` lädt Seed beim Erststart; neuer Build (frische `seedVersion`) frischt Gruppen-Profile auf, selbst angelegte bleiben. ⚠️ **Datenschutz:** öffentlich gehostet sind Namen+Tipps unter der URL abrufbar (Adrian informiert, für private Gruppe ok).
- **Responsive (PWA-Hochformat, iterativ mit Adrian am Gerät):** (a) Burger-Drawer statt linker Sidebar ≤680px (`global.css`, `App.tsx` menuOpen/Backdrop; Electron breit = unberührt). (b) `@media (max-width:680px)`-Block am **Ende von components.css** (lädt nach global → gewinnt): globales `overflow-x` clip; Karten-Grids → 1 Spalte (Stadien war minmax(420px)!, Spielplan, Live-Tabellen, Profile) bzw. 2 (Teams/Torschützen/Kader/Erfolge); **Match-Zeilen (`.liverow`/`.tiprow`/`.chronikmatch__row`) brechen via `nth-child`+grid-areas um** (Info oben · Heim–Score–Auswärts Mitte · Tipp/Status unten) statt zu stapeln; Team-Detail-Kopf stapelt + Wappen `position:static`; matchcard, lbrow, grouptable, newslist kompakter; Flip-Front `transform:rotateY(0) translateZ(0)` gegen durchscheinende Rückseite (Safari).
- 🔴 **OFFEN/unverifiziert (Bridge sehe ich nicht visuell; Build/Auslieferung/typecheck verifiziert):** finale Geräte-Sichtung ALLER Screens im Hochformat; **Spielerkarten-Flip-Spiegelung** (Safari-3D, Fix unsicher — falls noch da, Ursache vermutl. Foil-Effekt auf der Front: HoloSticker/FoilStickerCard-CSS auf `filter`/`mask` prüfen); **GitHub-Pages-Deploy** steht aus (Arbeitsverzeichnis ist kein Git-Repo); voller iPhone-Install/Offline-Test braucht https (LAN-http hat keinen Service-Worker).

### Release v1.8.11 — KO-Tipp-Hilfe („Form & Quoten") + deutsche Menüleiste ✅ (16.06.; Auslöser: Adrian „auf Release 1.8.11 hinarbeiten", nach Entscheid Feature-Punkt 2 vor PWA)

- **KO-Tipp-Hilfe (Feature-Punkt 2, Memory `projekt-feature-ideen`):** Unter jeder KO-Tippzeile (KoEinstieg) ein dezenter Toggle „Form & Quoten", der ein **Vergleichs-Panel** aufklappt — beide Teams nebeneinander. Pro Team: **Herkunft** („Gruppensieger E" etc., aus dem rank in der echten Tabelle), **volle Gruppentabelle** (echte Ergebnisse via `realBracket.tables`, betreffendes Land blau hervorgehoben), **Turniersieg-Quote** (`ODDS.odds`/`formatOdds`) + **Favoriten-Hinweis** (niedrigere Quote, Panini-Blau — kein Gold, Belohnung bleibt reserviert). Team noch nicht fest (spätere Runden/Platzhalter) → dezentes „Steht noch nicht fest" statt Tabelle.
- **Architektur (Reuse, null Regression):** Neue Komponente `KoTipMatch.tsx` **hüllt** `TipRow` (TipRow unangetastet → Gruppenphase unberührt) + eigener Aufklapp-State. `GroupTable` nur um optionales `highlight`-Prop erweitert (Zeile des Teams → `.grouptable__row--you`). KoEinstieg reicht `realBracket.tables` durch. Quoten = **odds.json Turniersieg-Snapshot** (BetMGM); ESPN `pickcenter` (pro Spiel) bewusst NICHT genutzt (Netz-abhängig, nur kurz vor Anpfiff, mehr Aufwand — Turniersieg-Quote ist beim Vorab-Tippen robuster, Adrian abgenommen). CSS strikt über Tokens.
- **Menü auf Deutsch (Ticket Adrian 15.06. erledigt):** Electron-Default war englisch (kein Menü gesetzt → `STATUS`-Ticket). `setGermanMenu()` (in `app.whenReady`, vor createWindow) setzt via `Menu.setApplicationMenu(Menu.buildFromTemplate([...]))` eine vollständige deutsche Leiste: App-Menü (Über/Dienste/Ausblenden/Beenden), Bearbeiten, Ansicht (Zoom/Vollbild), Fenster. `role` liefert Funktion+Shortcut, `label` explizit deutsch (Electron lokalisiert role-Labels nicht). **Am Gerät zu sichten** (macOS-Menüleiste ist NICHT im Fenster-Capture) — im Dev sofort via `npm run dev`.
- **Verifiziert (Seed-Screenshot):** neuer **Shot 28 `ko-hilfe.png`** (KoEinstieg, erstes Panel aufgeklappt). Damit echte Tabellen sichtbar sind, **Shot-Seed um Gruppen B+E vervollständigt** (11 Gruppenspiele in `seedShotData`, nur Screenshot-userData — echte App/Tipps unberührt): Panel zeigt GER (Gruppensieger E, 7:2/9) vs SUI (Gruppensieger B, 6:1/9), Quoten 15.00 vs 67.00, Favorit Deutschland, beide hervorgehoben. **Nebeneffekt:** Shot 8 (live-tabellen) zeigt B+E jetzt voll. `typecheck` grün; Shot-Lauf fehlerfrei (28 Shots), Shot 9 (live-baum) intakt.
- 🧠 **OFFEN — echter Live-Fall (Henne-Ei):** KO-Tipp-Hilfe wirkt in der echten App erst, wenn (a) 1.8.11 installiert UND (b) echte Gruppen durch (**ab 28.06.**) — davor folgerichtig Platzhalter. Deutsches Menü am Gerät noch zu sichten.
- **Release-Schritte:** package.json 1.8.10 → **1.8.11**; `npm run dist` (arm64, ad-hoc `identity:-`); Wächter vor `--dmg`: genau EINE `1.8.11`-.dmg (158 268 277 B), 0× Martin/entsperrt; `publish:update -- --notes '…' --dmg`. **Daten v28** (inkl. coaches.json mit TUN-Interim). Am Endpunkt verifiziert (Manifest 1.8.11/v28, Direktlink-Asset 158 268 277 B = lokale .dmg, Notes korrekt). arm64-only (Adrian-Entscheid).
- **Doku-Korrektur (Adrian 16.06.: „martin ist drin, läuft doch"):** Der STATUS-Wächter „Martin braucht entsperrten Nachzügler-Build" war VERALTET — Martin läuft regulär mit (Rückstand längst aufgeholt), Sonderbuild (`VITE_MARTIN`) nicht mehr nötig. Martin-/Nachzügler-Einträge aus STATUS entfernt. Lehre: beim Neuschreiben von STATUS übernommene Wächter gegen den Ist-Stand prüfen, nicht blind fortschreiben.

### Release v1.8.10 — Spielernamen vollständig (kein „…"-Abschnitt) ✅ (15.06.; Auslöser: Adrian „die nachnamen werden bei der aufstellung mit ... abgekürzt. warum?")

- **Ursache:** `.pitch__pname` hatte `max-width:66px` + `white-space:nowrap` + `text-overflow:ellipsis` ⇒ lange Kurznamen („O. Marmoush", „K. De Bruyne", „C. De Ketelaere") wurden abgeschnitten. Reine Optik, Daten waren vollständig.
- **Fix:** `.pitch__pname` → `max-width:76px`, `overflow-wrap:break-word`, `line-height:1.15`, `text-align:center`; nowrap/ellipsis/overflow-hidden raus. Lange Namen brechen auf 2 Zeilen, kurze bleiben 1-zeilig. 76px gewählt als Balance (lange noch lesbar, in 5er-Reihen keine horizontale Kollision). Ghost-Name erbt das Umbruch-Verhalten.
- **Verifiziert:** Seed temporär mit „C. De Ketelaere"/„O. Marmoush" bestückt → Shot zeigt beide voll (De Ketelaere sauber 2-zeilig), keine Überlappung; Seed danach zurückgesetzt. `typecheck` grün.
- **Release:** package.json 1.8.9 → **1.8.10**; Changelog 1.8.10; `npm run dist`; genau EINE `1.8.10`-.dmg (158 263 507 B); `publish:update -- --notes '…' --dmg`. **Daten v27**. Am Endpunkt verifiziert (Manifest 1.8.10/v27, Asset = lokale .dmg).

### Release v1.8.9 — HOTFIX Aufstellungen (alle Spieler transparent + beide Pfeile) ✅ (15.06.; Auslöser: Adrian „die letzte session hat die aufstellungen zerhackt … jeder spieler transparent und mit ein- und auswechselpfeil … das ist scheisse" + Screenshot EGY–BEL)

- **Symptom:** Am ECHTEN Spiel (EGY–BEL, vor Anpfiff) bekam JEDER Spieler grünen ▲ UND roten ▼ und war gedimmt; Bank alle mit ▲. Der Seed-Test (1.8.8) zeigte es nicht.
- **Ursache:** ESPN liefert `roster[].subbedIn`/`subbedOut` UNEINHEITLICH — im laufenden Spiel als **Boolean** (`false`/`true`), vor Anpfiff als **Objekt** `{didSub: false}`. Ein Objekt ist in JS truthy ⇒ `if (e.subbedIn)` war für jeden Spieler wahr. (Die 1.8.8-Verifikation hatte nur das LAUFENDE ESP–CPV geprüft, wo die Felder Booleans waren — der Vor-Anpfiff-Zustand mit `{didSub}` blieb ungetestet.)
- **Fix:** `didSub(v)`-Helper in `src/shared/results.ts` (true wenn `v===true` ODER `v.didSub===true`), `toLineupPlayer` nutzt `didSub(e.subbedIn/Out)`; `EspnRosterEntry.subbedIn/Out` Typ → `boolean | { didSub?: boolean }`. `subbedInFor.jersey` + Minute aus `plays` unverändert korrekt.
- **Verifiziert an ECHTEN Daten (beide Zustände):** EGY–BEL vor Anpfiff → subbedIn=0/subbedOut=0 (keine Pfeile, voll deckend); ESP–CPV laufend → nur echte Wechsel mit korrekten Minuten (ESP 4/4, CPV 5/5). Shot 27 zeigt Seed weiterhin korrekt (nur 2 echte Wechsel/Team). `typecheck` grün.
- **Lehre (→ Memory `feedback-verifikation-am-echten-fall`):** Parsing externer Feeds gegen ECHTE Daten in ALLEN relevanten Zuständen prüfen (vor Anpfiff ≠ live ≠ beendet) — nicht nur Seed oder einen einzigen Spielstand.
- **Release-Schritte:** package.json 1.8.8 → **1.8.9**; Changelog-Eintrag 1.8.9 (Darstellung korrigiert); `npm run dist` (arm64); genau EINE `1.8.9`-.dmg (158 269 035 B), 0× Martin; `publish:update -- --notes '…' --dmg`. **Daten v26** — enthält jetzt erstmals `coaches` (OTA-Trainer ab 1.8.9 scharf). Am Endpunkt verifiziert (Manifest 1.8.9/v26, Asset = lokale .dmg, coaches-Anzahl 48).

### Trainer-Watcher + OTA-Plumbing für coaches.json ✅ (15.06., direkt nach 1.8.8; Auslöser: Adrian „ach krass, hab ich gar nicht mitbekommen!!! kannst da keinen watcher einbauen?")

- **Cloud-Routine erstellt** `trig_01F2EfZDT3kCTKLPaB6dYiKL` „WM26: Trainerwechsel bei den 48 WM-Teams überwachen", cron `0 6 * * *` (08:00 MESZ), sonnet-4-6, Tools Bash/WebSearch/WebFetch, **kein Repo** (self-contained: Baseline-Trainerliste im Prompt), kein MCP — gespiegelt an der Nati-Fotos-Routine. Prüft täglich alle 48 Trainer gegen die Baseline, meldet NUR Abweichungen (Quelle + Konfidenz + kopierfertiger coaches.json-Schnipsel), pflegt nichts selbst ein; dubiose Quellen (cryptobriefing.com & Co.) explizit ausgeschlossen. Erster Lauf 16.06. ⚠️ Baseline steckt im Routine-Prompt ⇒ bei coaches.json-Änderung Prompt via `RemoteTrigger update` nachziehen.
- **OTA-Plumbing für Trainer** (damit ein Wechsel ohne App-Rebuild ausspielbar wird): `DataRelease.coaches` (shared/types.ts), `applyInfoOverride` mergt `coaches` in eine mutierbare COACHES-Kopie (info.ts), `publish-update.mjs` nimmt `coaches: readJson('resources/data/coaches.json')` ins Daten-Release. **Henne-Ei:** wirkt erst ab der App-Version, die diesen Code enthält (≥1.8.9) — die laufende 1.8.8 hat Trainer nur gebündelt ⇒ der ERSTE künftige Wechsel braucht noch einen Rebuild (auf 1.8.9), danach sind Trainer reine Daten-Updates. Typecheck grün. NICHT separat released (reitet auf dem nächsten Release mit).

### Release v1.8.8 veröffentlicht — Aufstellungen: volle Bank, Auswechslungen & Trainer ✅ (15.06.; Auslöser: Adrian „nimm die transparenz raus … werden auswechslungen angezeigt? MUSS … alle bankspieler plus trainer … home links away rechts … eingewechselte auf dem feld mit pfeil rein", dann „hau das ding rauf")

- **Drei Bitten + Klärung:** (1) „Transparenz raus" — es gab NIE eine foto-basierte Transparenz; die gedimmten Spieler waren die Ausgewechselten. Jetzt hat das Dimmen klare Bedeutung (rotes ▼). (2) Auswechslungen MÜSSEN rein — ESPN liefert sie. (3) „nur 3 CPV-Spieler links" — Ursache: Parser behielt **nur Eingewechselte** (`subbedIn`); war eine Seite leer, rutschte die andere nach links.
- **ESPN-Feed am Live-Spiel ESP–CPV (Event 760428) inspiziert:** `rosters[].roster` führt ALLE 26 (11 Startelf + 15 Bank); Eingewechselte tragen `subbedInFor.jersey` (für wen) + `plays[].clock.displayValue` (Minute, z. B. `71'`); Ausgewechselte `subbedOut`. **Trainer fehlt komplett** — nicht im `summary`, nicht im core-API-`competitor`, `coaches`-Endpoint 404. ⇒ eigene Datendatei nötig (Adrian-Entscheid).
- **Parser** (`src/shared/results.ts`): `toTeamLineup` behält jetzt die **ganze Bank** (nach Nr. sortiert), `toLineupPlayer` zieht `outMinute`/`inMinute`/`forNo` aus `subMinutes()` + `subbedInFor`. Typen (`shared/types.ts`): `LineupPlayer` um `outMinute/subbedIn/inMinute/forNo` erweitert, `TeamLineup.subs` → **`bench`** (volle Bank). Logik per Python gegen die echte Live-Response geprüft: ESP 2 Wechsel-Slots, CPV 4, alle korrekt gepaart inkl. Minute.
- **Darstellung** (`FormationPitch.tsx` + CSS): Slot = der **aktuelle** Spieler ankert am Punkt (Feld-Elf bleibt korrekt positioniert), Ausgewechselte floaten als kleine, gedimmte „gefächerte" Ghost-Chips DARÜBER (`.pitch__gone`, `--compact`, `margin-bottom:-11px`) — gewählt, weil in dichten Formationen nur ~6px zwischen den Reihen sind und ein zweiter voller Chip sonst in den Nachbarn ragt (erst getestet: Appollis-Ghost lag auf Mokoena). Rotes ▼ / grünes ▲ (`--live-red`/`--pitch-green`) mit Minute; das Badge bleibt voll lesbar (nur Nummer+Name dimmen, nicht das ganze Chip). **Bank** (`.pitch__bench`): volle Ersatzbank beider Teams, **Heim links / Auswärts rechts** (`justify-content:space-between`, away gespiegelt), Namen klickbar → Panini-Karte, Eingewechselte mit grünem ▲+Minute. **Trainer-Zeile** je Bank-Spalte.
- **Trainer-Daten:** neu `resources/data/coaches.json` (Trigramm → Name, alle 48), Loader `lib/info.ts → coach()`. NICHT im Daten-Release (das sammelt nur squads/teaminfo/stadiuminfo/odds) ⇒ Trainer reist **im App-Binary** mit, nicht via Override. Recherche per **4 parallelen Agenten** (je 3 Gruppen, Wikipedia + Cross-Check, high confidence): u. a. KSA Georgios Donis (Renard 04/26 entlassen), CZE Koubek, MAR Ouahbi, BEL Garcia, BRA Ancelotti, ENG Tuchel, POR Martínez, UZB Cannavaro. **⚠️ TUN volatil:** Lamouchi am 15.06. nach 1:5 vs. SWE entlassen, **Mondher Kebaier** interim (Sky/Goal/WST bestätigt) — eingetragen, aber im Auge behalten.
- **Release-Schritte:** package.json 1.8.7 → **1.8.8**; Changelog-Einträge 1.8.8 **und nachgereicht 1.8.7** (fehlte!); `npm run dist` (arm64, ad-hoc `identity:-`); Wächter vor `--dmg`: genau EINE `1.8.8`-.dmg (158 268 983 B), 0× Martin/entsperrt; `publish:update -- --notes '…' --dmg`.
- **Verifiziert (Artefakt + GitHub direkt):** `typecheck` grün; Shot-Lauf ohne Fehler, **Shot 27 `aufstellung.png`** zeigt gestapelte Wechsel (Chávez ▼63 → Antuna ▲63, Appollis ▼70 → Mahlangu ▲70), volle Bänke (Trainer Aguirre/Broos, home links/away rechts). Manifest am Repo **App 1.8.8 · Daten v25**; Direktlink-Asset `WM26-Tipp-arm64.dmg` **158 268 983 B = lokale .dmg**. arm64-only (wie 1.8.3–1.8.7). `data:schedule` blieb gesperrt.
- 🧠 **OFFEN — Erlebnis-Check am echten Fall** (Memory `feedback-verifikation-am-echten-fall`): (1) 1.8.8-Banner am Gerät (CDN-Lag); (2) **voller Live-Pfad** mit ECHTER ESPN-Aufstellung+Wechsel im laufenden App — bisher nur Seed + Parse-Logik gegen echte Response geprüft. Erster echter Test heute Abend ~1 h vor EGY–BEL (21:00) / KSA–URU (24:00 MESZ): Live-Zeile aufklappen, beide Bänke + ggf. Wechsel sauber? Im Dev jederzeit via `npm run dev` (Poller holt auch dev echte Daten).

### Release v1.8.7 veröffentlicht — Aufstellungs-Spielfeld (Live-Lineups) ✅ (15.06.; Auslöser: Adrian „woher kommen die live-daten? … wäre geil, wenn die aufstellungen super sexy im design beim aufklappen erscheinen", dann „die 1. … Release ≥1.8.7 für heute Abend")

- **Datenquelle bestätigt:** Live-Ergebnisse = **ESPN** (`site.api.espn.com`, keyless; Fallback fixturedownload). Aufstellungen liefert ESPN gleich mit — `summary?event=<id>` (~370 KB), am echten Feed verifiziert (15.06., Spiel ESP–CPV live): `rosters[].formation` ("4-3-3"), pro Spieler `jersey`, `athlete.shortName/displayName`, `position.abbreviation` (G/CD-L/RB/DM…), `subbedIn/Out`. **Startelf erscheint ~1 h vor Anpfiff** (Vorspiel 2 h vorher = leere rosters → bestätigt). `summary` führt zusätzlich `standings` (Gruppentabelle) + `pickcenter` (DraftKings-Quoten) — für Feature-Punkt 2 reserviert.
- **Architektur:** neuer Fetch-Pfad im Poller `src/main/results.ts → pollLineups()`: holt summary GEZIELT im Anstoß-Fenster (75 min vor bis 160 min nach Anpfiff), pro Spiel max. alle 4 min (Last-Bremse), beendete Spiele mit Aufstellung nie wieder; `nextLineupWakeMs()` weckt den Loop rechtzeitig (entkoppelt vom Scoreboard-Takt). Persistenz in `wm26-results.json` (Feld `lineups`, abwärtskompatibel `?? {}`), Broadcast `lineups:update`. Parsing `src/shared/results.ts → parseEspnLineups` (Heim/Auswärts wie `espnToLiveResult`: Trigramm in Gruppe, sonst `homeAway`). IPC `lineups:get` + Preload `getLineups/onLineups` + Store `lineups`/`useLineup`.
- **Darstellung:** `components/FormationPitch.tsx` + `lib/formation.ts` — Layout deterministisch aus Formation (Reihen) + Positionskürzel (Tiefe + Links/Rechts), **NICHT** aus ESPNs verschachteltem `formationPlace`; Logik gegen echte ESP/CPV-Daten von Hand geprüft. Beide Startelfen auf einem Panini-Flutlicht-Rasen (SVG-Linien), Heim unten/Auswärts gespiegelt, blau vs. magenta Sticker-Chips, Flagge+Formation im Kopf, Ausgewechselte gedimmt, Einwechslungen darunter. Chip klickbar → `PlayerPeek` (Brücke wie Torschützen, `findSquadPlayer`). Eingehängt in `LiveRow` (Aufklappen jetzt auch bei verfügbarer Aufstellung; Pitch über dem Tipp-Vergleich), durchgereicht in `Live.tsx` + `Heute.tsx`. CSS `.pitch*` in components.css (Tokens, `--pitch-green`).
- **Produkt-Entscheidung (Adrian 15.06.):** Wettquoten NICHT in die Live-Aufstellung — sie wandern zu Feature-Punkt 2 (KO-Tipp-Hilfe: Gruppenresultate + Tabelle + Quoten als Entscheidungshilfe, vor 28.06.).
- **Release-Schritte:** package.json 1.8.6 → **1.8.7**; `npm run dist` (arm64, ad-hoc `identity:-`); Wächter vor `--dmg`: genau EINE `1.8.7`-.dmg in `dist/`, 0× Martin/entsperrt. `publish:update -- --notes '…' --dmg` (Notes in EINFACHEN Quotes, „—" intakt).
- **Verifiziert (Artefakt, GitHub direkt):** `typecheck` grün; Shot-Lauf ohne Fehler, neuer **Shot 27 `aufstellung.png`** zeigt den Pitch (MEX 4-3-3 vs. RSA 4-2-3-1 aus Seed, gedimmter Chávez, Sub Antuna). Manifest am Repo **App 1.8.7 · Daten v24**; Release-Assets `WM26-Tipp-arm64.dmg` (ewiger Link) + `WM26.Tipp-1.8.7-arm64.dmg`, beide **158 264 669 B = lokale .dmg**; Direktlink `releases/latest/download/…` HTTP 200. arm64-only (wie 1.8.3–1.8.6). dataVersion +1 = v24 (Daten unverändert, `data:schedule` blieb gesperrt). Nachzügler-Pre-release unberührt.
- 🧠 **OFFEN — zwei Erlebnis-Checks am echten Fall** (Memory `feedback-verifikation-am-echten-fall`): (1) **1.8.7-Banner am Gerät** (CDN-Lag wie immer; Profile → „Nach Updates suchen" bzw. Neustart). (2) **Voller Live-Pfad**: echte ESPN-Aufstellung → Pitch wurde NUR mit Seed-Daten + gegen die echte ESPN-Response-Struktur geprüft, nie mit einer echten Live-Aufstellung im laufenden App. Erster echter Test heute Abend ~1 h vor EGY–BEL (21:00) / URU–KSA (24:00 MESZ).

### Release v1.8.6 veröffentlicht — Siegerehrung + Stadionjubel ✅ (15.06.; Auslöser: Adrian „jetzt stimmts … hau das ding rauf")

- **Inhalt:** Siegerehrungs-Screen (Adrian abgenommen) + Volltreffer-Stadionjubel (s. Detail-Blöcke unten). Davor noch auf Adrians Frage „was ist gesamt/haupt/ko?" reagiert: **Erklär-Hint** unter die Podest-Überschrift gesetzt (Gesamt = Haupt + KO; Haupt = ab Spiel 1; KO = Einstieg ab 1/16) **und Demo-Daten-Bug gefixt** (bei >5 Profilen wiederholten sich die Mock-Punkte via `i % pts.length` ⇒ zwei Profile zeigten identisch 185/131·54; jetzt index-basiert eindeutig fallend — betraf NUR die dev-Vorschau, nie die echte Wertung).
- **Release-Schritte:** `package.json` 1.8.5 → **1.8.6**; Changelog-Eintrag „Siegerehrung & Stadionjubel"; `npm run dist` (arm64, ad-hoc `identity:-`); **Wächter vor `--dmg`** geprüft: genau EINE `1.8.6`-.dmg in `dist/`, Martin/Entsperr-Code 0× im Bundle (getreeshaket, Sperre aktiv), Siegerehrung/Triple/`cheer.mp3` drin. `publish:update -- --notes '…' --dmg` (notes in EINFACHEN Quotes ⇒ typografische „" unbeschädigt).
- **Verifiziert (Artefakt, GitHub direkt = ohne raw/Fastly-Lag):** `typecheck` + Production-Build grün; Manifest im Repo **App 1.8.6 · Daten v23**; Release-Assets `WM26-Tipp-arm64.dmg` (ewiger Link) + `WM26.Tipp-1.8.6-arm64.dmg`; Direktlink `releases/latest/download/WM26-Tipp-arm64.dmg` HTTP 200, content-length **158 274 183 = lokale .dmg**. arm64-only (wie 1.8.4/1.8.5; kein Intel-Bedarf gemeldet). dataVersion +1 = v23 (Daten unverändert, nur Laufnummer; `data:schedule` blieb gesperrt ⇒ 00:30Z/03:00Z-Patch intakt). Nachzügler-Pre-release unberührt.
- 🧠 **OFFEN — Erlebnis-Check am echten Gerät** (Memory `feedback-verifikation-am-echten-fall`): raw/Fastly liefert nach Publish Minuten lang das ALTE Manifest (CDN-Lag, `?t=` hilft nicht). Adrian muss in ein paar Minuten am echten Gerät prüfen, dass das „1.8.6"-Banner ankommt (Profile → „Nach Updates suchen" bzw. Cmd+Q + Neustart). Bis dahin gilt NUR das Artefakt als verifiziert, nicht das Nutzererlebnis.

### Siegerehrungs-Screen gebaut (Sidebar-Index 12) ✅ gebaut & per Shot verifiziert — Adrians Live-Abnahme + nächster Release offen (15.06., Feature-Idee 2/3)

- **Adrians Wertungsmodell** (per Frage abgenommen): drei Krönungen — **Hauptsieger** = Haupt + KO zusammen · **Sieger Haupt** = nur `main` · **Sieger KO** = nur `fromR32`. Gewinnt EINE Person alle drei → **Triple-Superscreen** (👑). „KO" = bewusst **nur `fromR32`** (der gemeinsame KO-Durchtipp ab 1/16, den laut Plan alle ab 28.06. tippen); `fromR16/QF/SF` sind Nachzügler-Nischen und bleiben aussen vor — falls Adrian sie doch will, hier nachziehen.
- **Zwei Zustände** (Finale erst 19.07.): VOR Finale = Vorfreude-Bühne (🏆-Teaser + Live-Countdown bis Finale + aktuelle Gesamt-Führung als dezentes Podest, kein Gold); NACH Finale (`results[104].status==='finished'`) = volle Zeremonie (Weltmeister-Banner mit Flagge + „wer hat ihn getippt", Gold-Podest 2·1·3 mit `HoloSticker gold` auf Platz 1, Rang 4+ Liste, zwei kleine Sieger, Dauer-Konfetti).
- **Dateien:** neu `screens/Siegerehrung.tsx` + CSS-Block in `components.css` (`.cer-*`, `.podest*`; Tokens/`.foil`/`confettifall`-Keyframe wiederverwendet). Registriert in `App.tsx` (RUBRIKEN + SCREENS, **Index 12 hinten** — Profile bleibt 11, Shot-Hook-Indizes 0–11 unberührt).
- **Daten:** `computeBreakdown` je Profil für `main` (SCHEDULE) und `fromR32` (`entrySchedule`/`resolveLateBracket`, bonusRounds der Kategorie); `combined = main.total + ko.total`; `real.champion` aus `buildRealWorld`; Champion-Tipper via `bd.durchtipp.champion`. Sortierung wie Rangliste (Punkte→exact→Name).
- **Verifikations-Vorschau (dev/shot-only):** `import.meta.env.DEV`-gegateter Demo-Schalter (Buttons „Zeremonie"/„Triple") mit Mock-Standings + Demo-Champion ESP — im Release tree-geshaket (`effectiveDemo='off'`), Demo-Code nie ausgeführt. Der **Triple-Superscreen selbst ist echtes Feature** (greift bei echtem Triple). Shot-Hook (`src/main/index.ts`) um Index 12 erweitert: Shots 24 Vorschau · 25 Zeremonie · 26 Triple.
- **Verifiziert:** `typecheck` + `npm run build` (production, DEV=false) grün; **3 Shots** geprüft — Vorschau (Countdown korrekt „34 Tage" bis 19.07., echte Live-Führung), Zeremonie (Spanien-Champion, Podest, Mini-Sieger), Triple (👑 + Konfetti, Shot-Test gewinnt alle drei). 🧠 OFFEN: Adrians Design-Abnahme am laufenden dev; greift für andere erst ab nächstem Release; der echte Post-Finale-Pfad ist erst am 19.07. real testbar (bis dahin nur Demo-verifiziert).

### Volltreffer-Jubel final: Applaus-Platzhalter → ElevenLabs-Stadion-Roar ✅ (15.06., von Adrian per Kostprobe abgenommen)

- **Auslöser:** Feature-Idee „Jubel-Sample feinschleifen" (STATUS/Memory) — `cheer.ogg` war ein PD-Applaus-Platzhalter (Wikimedia), kein Stadion-Torjubel.
- **Neue Datei:** Adrian erzeugte `volltreffer.mp3` mit **ElevenLabs** (Stadion-Roar, 48 kHz/192 kbps Stereo, ~10 s). Eingebaut als `src/renderer/src/assets/sound/cheer.mp3`.
- **Verifikation der Eignung VOR Einbau (ffmpeg-Hüllkurve pro Sekunde):** Peak bei 1–2 s (−5,8 dB), laut bis ~3 s, danach langer Ausklang bis −49 dB → Roar sitzt genau am Anfang. Datengestützt **Cutoff `playCheer` 2600 → 3000 ms** erhöht (`lib/sound.ts:56`), damit der ganze laute Roar (0–3 s) durchläuft, bevor er ausblendet; bleibt innerhalb der 4-s-`RewardMoment`-Karte. Lautstärke unverändert 0.55.
- **Format-Umstellung:** Import in `lib/sound.ts:3` von `cheer.ogg` → `cheer.mp3` (Vite löst Asset über Endung auf), alte `.ogg` (545 kB) gelöscht. `CREDITS.md` aktualisiert (cheer → Eigenes Werk/ElevenLabs/15.06.).
- **Verifiziert:** `typecheck` grün; `npm run build` grün, `cheer-*.mp3` (241.39 kB = Dateigröße) als Asset gebündelt, alte .ogg raus, keine Restreferenzen; dev-Start ohne Fehler. **Hör-Kostprobe** (dev-Build, Profile → Ton aus/an löst `playCheer()`): von Adrian abgenommen („klingt gut"). 🧠 Wirkt für andere Nutzer erst ab nächstem Release (Sound geht mit dem nächsten Code-Release raus).

### Nati-Fotos: 8 offene manuell auf Commons geprüft — nichts Sauberes, Vargas-Fund verworfen ✅ (15.06., Auslöser: STATUS-Wächter „Nati-Spielerfotos einpflegen", 8 offen)

- **Automatischer Weg bestätigt leer:** `node scripts/fetch-squad-photos.mjs SUI` → „0 neu · 18 vorhanden · 8 ohne freies Bild". Das Skript nimmt nur das **Wikidata-P18**-Bild eines Items mit Beschreibung „footballer" — junge/neu nominierte Spieler haben oft kein P18. Cloud-Routine (täglich 09:00) hatte also zu Recht nichts gemeldet.
- **Manuelle Commons-Dateisuche** (über das Skript hinaus) für alle 8: nur **Vargas** hat überhaupt ein frei lizenziertes Foto — `File:Ruben Vargas SFV 2019.jpg`, CC BY 3.0, 512×683, Autor SFV. Rest: Manzambi/Aebischer/Keller/Jaquez = kein Foto (nur PDFs); „Fabian Rieder" = gleichnamiger **Rennrodler**; Amenda/Itten = nur YB-**Mannschafts**fotos (Crop + Person-ID nötig).
- **Vargas verworfen (Adrian entschieden):** Bild ist ein **YouTube-Interview-Standbild** (Beschreibung nennt 2 Personen: Vargas + Sidler), seitlicher Blick, weich, Sofa-Hintergrund — nicht der Panini-Studio-Look; dazu Rest-Unsicherheit, ob der im width=512-Crop Sichtbare Vargas oder Sidler ist. Linie: kein qualitativ schwaches/unsicheres Foto ins Album.
- **Ergebnis:** nichts eingepflegt, kein Code-Release. 8 bleiben offen, Cloud-Routine läuft weiter. 🧠 **LEHRE:** Beim nächsten Foto-Block nicht erneut blind die manuelle Commons-Suche fahren — Stand 15.06. ist erschöpft; sinnvoll erst wieder, wenn die Cloud-Routine ein neues freies Einzelporträt meldet.

### Update-Banner-Zuverlässigkeit gehärtet ✅ (14.06., **veröffentlicht als v1.8.5, Daten v22**; Auslöser: Bug-Ticket aus STATUS — „Update-Banner kommt unzuverlässig an": App auf alter Version neu starten ⇒ oft KEIN Dialog trotz live neuer Version)

- **Drei Wurzeln, drei Fixes (alle aus dem abgenommenen Ticket):**
  1. **Poll geschärft** (`update.ts`): `POLL_MS` 6 h → **2 h**; zusätzlich **Re-Check bei Fenster-Fokus** (`index.ts`: `app.on('browser-window-focus')` → neue `UpdateService.maybeRefresh()`, gedrosselt auf 15 min via `lastTickAt`/`FOCUS_THROTTLE_MS`). Deckt den daueroffenen Sofa-Begleiter ab: bei Fokus-Rückkehr frisch statt bis zu 6 h Verzug. `tick()` setzt `lastTickAt` beim Start (auch bei Fehlschlag ⇒ kein Fokus-Spam; RETRY_MS = 30 min greift weiter).
  2. **„Nach Updates suchen"-Button** (cache-busted Sofort-Check): neue `UpdateService.checkNow()` (ruft `tick()` ⇒ nutzt den vorhandenen `?t=${Date.now()}`-Cache-Buster, liefert frischen Snapshot zurück), IPC `update:check` (im Shot-Modus leerer Snapshot — kein Netz-Poll), Preload `checkUpdate()`, UI-Abschnitt „App-Version" im Profile-Screen (`Profile.tsx → UpdateCheck`): zeigt installierte Version + Button; Rückmeldung „Du hast bereits die neuste Version" bzw. „Version X ist da — siehe Banner oben" (Banner erscheint via Broadcast automatisch).
  3. **Dismiss gehärtet** (`App.tsx → UpdateBanner`): Exakt-Vergleich `dismissed !== appVersion` → `dismissed == null || cmpVersion(appVersion, dismissed) > 0`. Nur eine ECHT neuere als die weggeklickte Version meldet sich wieder ⇒ robust gegen wiederverwendete Nummern (war die 1.8.3-Falle).
- **Wirkungshorizont (wie schon der 1.8.4-Cache-Buster):** greift erst ab installiertem 1.8.5-Code; für draußen laufende Altversionen nichts rückwirkend — darum sofort als Release ausgeliefert. Der echte Gewinn gilt ab jetzt für KÜNFTIGE Updates.
- **Shot-Hook erweitert** (`index.ts → captureScreens`): Profile-Rubrik (Sidebar-Index 11) wird jetzt mitgeschossen (`23-profile.png`) — der Screen hatte bisher keinen Shot, hat nun interaktive Elemente (Sound, Update-Check). Profil-Screen per Shot visuell verifiziert: App-Version-Abschnitt sitzt sauber unter „Ton", Tokens konsistent (`sectiontitle`/`lead`/`btn`).
- **Changelog:** neuer 1.8.5-Eintrag „Updates auf Knopfdruck" (Nutzersprache).
- **Verifiziert:** `typecheck` grün (beide Projekte); Build arm64 ad-hoc-signiert; **Wächter** vor Publish: genau eine `1.8.5`-.dmg in `dist/` (normal/gesperrt — „Nachtipp-Fenster" 0× im Bundle, Martin-Code getreeshaket). Release `v1.8.5` (kein Pre-release ⇒ Latest), Assets `WM26-Tipp-arm64.dmg` (ewiger Direktlink, HTTP 200, content-length 158 594 435 = lokale .dmg) + `WM26.Tipp-1.8.5-arm64.dmg`; Live-Manifest App **1.8.5** / Daten **v22** / Notes ✓. Nachzügler-Pre-release unberührt. Daten v22 = unveränderter Ist-Stand (keine Stammdaten-Änderung; `data:schedule` blieb gesperrt ⇒ 00:30Z/03:00Z-Patch intakt). 🧠 **LEHRE (Shell):** `--notes` mit typografischen „…" nur in EINFACHEN Quotes übergeben — doppelte Quotes außen brechen am eingebetteten `"` (zsh `unmatched "`).
- **⚠️ NACHTRAG — meine Verifikation war zu oberflächlich (Auslöser für Adrians „was zur hölle"):** Geprüft war nur das Release-Artefakt; bei Adrians REAL laufender 1.8.4 kam das Banner **nicht**. Diagnose an seinen echten Daten (`~/Library/Application Support/WM26 Tipp/wm26-update.json`): `fetchedAt 2026-06-14T20:29:39Z` = sein Start-Poll lief **~4 Min nach** meinem Manifest-Upload und traf `raw.githubusercontent` noch im CDN-Lag (raw braucht nach Commit Minuten + Fastly `max-age=300`) ⇒ App cachte `appVersion 1.8.4` == laufend ⇒ `cmpVersion 0` ⇒ kein Banner. Danach pollt 1.8.4 erst in **6 h** wieder. Der „Nach Updates suchen"-Button (der eigentliche Schutz) ist erst ab 1.8.5 ⇒ half ihm auf 1.8.4 null. Der Cache-Buster `?t=` hilft hier nicht, weil das Lag CDN-/Propagationsseitig sitzt, nicht im Query-String. **Sofort-Abhilfe:** App Cmd+Q + neu starten ⇒ frischer Poll (Fastly liefert inzwischen sauber 1.8.5/v22) ⇒ Banner unten rechts. 🧠 **LEHRE: Erfolg am echten Nutzerfall am realen Gerät verifizieren, nicht am Artefakt; Henne-Ei-Fixes (greifen erst ab nächster installierter Version) offen ausweisen statt unter „alles grün" verbuchen** (Memory `feedback-verifikation-am-echten-fall`). ✓ Bestätigt: Adrian hat die App per Cmd+Q neu gestartet, das Banner kam unten rechts an — Diagnose & Sofort-Abhilfe verifiziert, Faden zu.

### Update-Banner repariert (1.8.3 verbrannt) + Start-Splash + Cache-Buster ✅ (14.06., **veröffentlicht als v1.8.4, Daten v21**; Auslöser: Adrian startete neu in 1.8.2 und bekam KEINEN Update-Banner; dazu „warum so langer schwarzer Startbildschirm?")

- **Banner-Bug — Wurzel: die Versionsnummer 1.8.3 war verbrannt.** Bestätigt am Code (`App.tsx → UpdateBanner`: `cmpVersion(appVersion,current)>0 && dismissed!==appVersion`) UND an Adrians echten Daten (Local-Storage-leveldb: `wm26-update-dismissed = 1.8.3`). Das Flag stammte vom TSCHUTTINI-Versuch (damals kurz als 1.8.3 released, „Später" geklickt → Flag, dann zurückgerollt). Da diese Session 1.8.3 WIEDERVERWENDET hatte, war `dismissed === appVersion` ⇒ Banner unterdrückt — bei jedem, der den TSCHUTTINI-1.8.3-Banner je weggeklickt hatte. **Fix: frische Nummer 1.8.4** (nirgends dismissed). 🧠 **LEHRE: Versionsnummern NIE wiederverwenden** — das Dismiss-Flag lebt im Local Storage weiter. 1.8.3 ist verbrannt.
- **Cache-Buster (`update.ts`):** raw.githubusercontent liefert via Fastly mit `cache-control: max-age=300` (5 Min); der App-Poll ist erst nach 6 h wieder dran ⇒ ein gecachtes altes Manifest beim Start kostet bis zu 6 h ohne Banner. Manifest- UND Daten-Abruf hängen jetzt `?t=${Date.now()}` an ⇒ garantierter Cache-MISS = immer frisch. Wirkt für Releases ab 1.8.4 aufwärts; per `curl ?t=` verifiziert, dass raw frisch 1.8.4 liefert.
- **Start-Splash (Adrians Wunsch):** Diagnose — das Hauptfenster wird sofort dunkel (`#0E1116`) gezeigt und bleibt schwarz, bis der Renderer (Bundle-Parse + Daten-IPC + Intro-Fade) malt; vor dem ersten JS kann nichts „melden", was lädt (darum keine granularen Stufen). Lösung: eigenes Sofort-Fenster (`index.ts → SPLASH_HTML`, Inline-HTML als `data:`-URL, System-Font, **inline-SVG-Emblem in Weiss** via `path{fill:#fff}`, „WM**26** TIPP" + blau→magenta Lade-Balken + „Das **Tschuttini**-Album wird aufgeschlagen …"). Hauptfenster `show:false`, erscheint bei `ready-to-show`, Splash schließt dann; **Sicherheitsnetz-Timeout 8 s** (nie im Splash hängenbleiben). Im Shot-Modus aus (`show:true`, deterministische Screenshots). Design von Adrian abgenommen (Browser-Vorschau + qlmanage-Render: weisses „26", großes T verifiziert).
- **Changelog:** neuer 1.8.4-Eintrag „Flotterer Start & zuverlässige Updates" (1.8.3-Eintrag „Der Live-Bereich, rundum geschärft" bleibt für alle, die direkt von ≤1.8.2 kommen).
- **Verifiziert:** `typecheck` grün; Build-Text-Check im asar (`Tschuttini-Album` 1×, kleines t 0×); `dist/`-Wächter (keine entsperrte .dmg). Release v1.8.4 (kein Pre-release ⇒ latest), Assets `WM26-Tipp-arm64.dmg` (ewiger Link) + `WM26 Tipp-1.8.4-arm64.dmg`; Manifest App **1.8.4** / Daten **v21** / Notes ✓. Nachzügler-Pre-release unberührt. ⚠️ Splash-Fenster-Mechanik ist NICHT per Shot-Hook testbar (zeigt bewusst sofort) — Fallback-Timeout sichert ab, visuell per Vorschau/Render geprüft.

### Live-Bereich geschärft + Anpfiff-Polling ✅ (14.06., **veröffentlicht als v1.8.3, Daten v20**; Auslöser: Adrian fragte, wo der Live-Holo-Rand „hin" sei → war nie weg, kam nur verzögert. Daraus ein 3-Achsen-Audit (Turnierlogik / Live-Robustheit / UX-A11y) und „mach den ganzen Wumms")

- **Anpfiff-Polling (der Auslöser):** Der Rand war nie verloren — er ist datengetrieben (erscheint, wenn ESPN `status:'live'` meldet) und wurde im 90-s-Takt gepollt, also bis zu 90 s nach Anpfiff. Neu `KICKOFF_POLL_MS = 25 s` im Fenster −5/+12 Min um den Plan-Anstoß (`shared/results.ts → nextPollDelayMs` in einem Durchlauf umgebaut; `nearKickoff` hat Vorrang vor `hasLive` ⇒ greift auch bei simultanen Anstößen am letzten Gruppentag). Logik an allen Zweigen durchgespielt. **Berührt den Nachzügler NICHT:** Tipp-Sperre = `isLocked(dateUtc) && !MARTIN_MODE` (anstoßzeit-basiert, `time.ts`), unabhängig vom Poller.
- **KO-Wertung = Endergebnis inkl. Verlängerung (Adrians Entscheid):** Tipp-Text fragte „nach 90 Min.", aber `scoring.ts → baseKind` verglich gegen den ESPN-Endstand inkl. Verlängerung — inkonsistent (nur bei einem ET-Tor relevant; Weiterkommer-Bonus + Baum waren korrekt, `buildRealWorld` nutzt den echten Sieger). Adrian wählte „Regel angleichen" statt 90-Min-Rekonstruktion (kicktipp-üblich, sofort verifizierbar, kein unverifizierbarer Rekonstruktions-Code vor der KO-Phase). Geändert: Regeltext `Rangliste.tsx`, BRIEFING §4 Z.39 + §4.4 Z.85, neue Entscheidung §11.12. Wertungscode unverändert.
- **Robustheit:** (1) Status-Rückfall-Wächter (`main/results.ts`) fängt jetzt auch `live/ht→scheduled` ab (nicht nur `finished`) — ein ESPN-Glitch kippt keine Live-Karte mehr auf „angesetzt". (2) Veraltet-Hinweis (`Live.tsx`): Bernstein-Banner bei >10 Min ohne ESPN-Daten **und** laufendem Spiel (Polls bleiben erfolgreich ⇒ alter Stale-Check allein greift nicht); neuer Token `--signal-amber` (Gold blieb der Belohnung). ✅ per Shot verifiziert.
- **Verschoben/abgesagt:** neues `postponed`-Flag (ESPN `state:'post'` ohne Wertung) → „verschoben" statt Anstoßzeit in LiveRow (Live + Heute). MatchCard/Spielplan bewusst ausgelassen (bräuchte Results-Threading, Beinahe-nie-Fall). `sameResult` um `postponed` ergänzt (sonst kein Broadcast).
- **Spielunterbrechung (Gewitter, US-Venues):** neues `delayed`-Flag — ESPN `state:'in'` + Stichwort `delay/suspend/abandon/interrupt` in `name`/`description` (Felder neu mitgelesen) → „⏸ Unterbrochen" statt eingefrorener Minute, Score bleibt, Holo-Rand steht still (`.liverow--delayed { animation:none }`). **Defensiv:** unbekannte ESPN-Bezeichnung ⇒ Verhalten wie bisher (kein Regress). Die exakte Fußball-Suspension-Bezeichnung ist am echten Fall noch ungesehen → Mehrfach-Stichwort. ✅ per Shot verifiziert (Mexiko 1:0 „Unterbrochen" neben normalem Halbzeit-Spiel).
- **Reduced-motion:** Audit meldete 6 Lücken — 5 davon schon durch den globalen Reset `global.css:86` (`*` Animation/Transition ~0) abgedeckt (Agent übersah die Regel). Echte Lücke nur das Intro (~20-s-JS-Timer, vom CSS-Reset nicht erreicht): überspringt jetzt bei `prefers-reduced-motion`, Overlay wird beim Start fokussiert ⇒ ESC wirkt sofort (war verdrahtet, nur Fokus fehlte). `Intro.tsx`.
- **Verifikation:** `npm run typecheck` grün (beide Projekte); voller Screenshot-Lauf (15 Screens, exit 0, Log sauber) als Smoke-Test; Stale-Banner, KO-Regeltext und „⏸ Unterbrochen" per **temporärem Seed** real per Shot geprüft (Seed-Zeitstempel/`delayed`/Flag danach jeweils zurückgesetzt — verifiziert).
- **Release v1.8.3 veröffentlicht + verifiziert:** `publish:update --dmg --notes …` (nur arm64; Mittipper Apple-Chip). Vorab: `gh` eingeloggt (Ceccaroni), `dist/` ohne entsperrte .dmg (Wächter). Live-Manifest App **1.8.3** / Daten **v20** / Notes ✓; Release `v1.8.3` (kein Pre-release ⇒ „latest"), Assets `WM26-Tipp-arm64.dmg` (ewiger Direktlink) + `WM26 Tipp-1.8.3-arm64.dmg`. Nachzügler-Pre-release unberührt. **Martin-Faden zu:** Adrian meldete „Martin ist durch" ⇒ Martin installiert via Banner die normale, **gesperrte** 1.8.3 = automatisch wieder zu, Tipps bleiben (= der geplante „zumachen"-Schritt, erledigt sich mit diesem Release).

### Nachzügler-Spezial: Sondertipp-Fenster für Spät-Einsteiger ✅ (14.06., Wunsch Adrian: Martin steigt spät ein, kann gelaufene Spiele nicht mehr tippen — Sonder-Build, die die Sperre öffnet, auf Ehrenbasis, mit humorvollem Einblender; Mechanismus bleibt für weitere Nachzügler)

- **Build-Schalter (kein Laufzeit-Flag, kein OTA!):** `electron.vite.config.ts` → `define: { __MARTIN__: VITE_MARTIN==='1', __LATE_NAME__: VITE_MARTIN_NAME ?? 'Martin' }`. `lib/martin.ts` exportiert `MARTIN_MODE`/`LATE_NAME`. **Jede normale Build = false** ⇒ der komplette Sonder-Code (Banner + Sperre-Aufhebung) wird per Tree-Shaking entfernt. Verifiziert: „Nachtipp-Fenster" 0× im Normal-Bundle, 1× im Martin-Bundle; `__MARTIN__`-Token 0× (ersetzt). Für alle anderen bleibt gesperrt — garantiert.
- **Sperre chirurgisch aufgehoben:** nur `TipRow.tsx` Z.30 `const locked = isLocked(...) && !MARTIN_MODE`. `isLocked` (lib/time.ts) selbst UNVERÄNDERT ⇒ Live-Reveal + „vergangen"-Optik intakt. `unresolved` (KO-Teams stehen strukturell noch nicht fest) bleibt für alle zu.
- **Einblender** `components/MartinModeNotice.tsx` (+ `.martinnote` in components.css; Panini-Blau/Gold, fixed top-center, wegklickbar): humorvolle Ehren-Mahnung „…trag ein, was du wirklich getippt hättest … sonst Strom durch die Tastatur auf die Pfoten ⚡🐾". Name aus `LATE_NAME`.
- **Gebaut + verteilt (Adrians Wahl „Option a"):** `VITE_MARTIN=1 VITE_MARTIN_NAME=Martin npm run dist` (arm64; Version temporär `1.8.2-martin` für eindeutigen DMG-Namen, danach zurück auf 1.8.2). Hochgeladen als **Pre-release** Tag `nachzuegler`, Asset `WM26-Tipp-Martin-arm64.dmg`. Verifiziert: „Latest" bleibt v1.8.2, Manifest unverändert (appVersion 1.8.2), Martin-Direktlink HTTP 200 anonym, ewiger Normallink unverändert (158 599 535 B). Nur Martin betroffen.
- **Nächster Nachzügler (Mechanismus bleibt):** `VITE_MARTIN=1 VITE_MARTIN_NAME=<Name> npm run dist` → Asset zum Pre-release `nachzuegler` hochladen (`gh release upload nachzuegler <dmg> --repo …`). ⚠️ Entsperrte .dmg **NIE in `dist/` lassen**, wenn `publish:update --dmg` läuft (globt `*.dmg` nach Version) — sonst landet sie im normalen Release. Nach dem Bauen aus `dist/` entfernt.
- **„Wieder zumachen":** Sobald der Nachzügler eingetragen hat (Adrian meldet sich), installiert dieser die normale 1.8.2 vom ewigen Link neu — Sperre zurück, die nachgetragenen Tipps bleiben (liegen im Store/userData).

### App-Umbenennung → „TSCHUTTINI '26" ⮌ VERSUCHT & WIEDER ZURÜCKGENOMMEN (14.06., Wunsch Adrian: App umtaufen, nur Anzeigename — „das Über und alles andere so lassen". Kurz als 1.8.3 released + 1.8.4 mit Auto-Aufräumen versucht, dann wegen Dock-Problem auf macOS 26 KOMPLETT zurück auf 1.8.2. **Nicht blind erneut versuchen — siehe Lehre unten.**)

- **Leitentscheidung:** Anzeigename (was man sieht) von technischer Identität (was die Maschine nutzt) entkoppeln — keine Datenmigration, kein neues `appId`, kein neues Dateiformat. Schreibweise von Adrian gewählt: **`TSCHUTTINI '26`** (gerader Apostroph, Versalien; passt zum bereits vorhandenen Tschuttini-Sidebar-Logo).
- **Sichtbar geändert:** `productName` (electron-builder.yml + package.json → Dock, Menü, .app-Bundle, DMG-Fenstertitel), BrowserWindow-`title` (index.ts), `<title>` (index.html), Onboarding-`<h1 class="foil">` (App.tsx), Anstoß-Notification (App.tsx), Chronik-PDF-Footer „erstellt mit …" (Chronik.tsx), iCal-`PRODID` (ics.ts).
- **Bewusst UNANGETASTET (technische Identität):** `appId: ch.lanz.wm26tipp` (Bundle-ID/OTA/Signatur), userData-Ordner **`WM26 Tipp`** (echte Tipps), `package.json` `name: wm26-tipp`, `.wm26tipp`-Format + Dialog-Label „WM26-Tipps", localStorage-Keys `wm26-*`, Store-Dateien `wm26-store.json`/`wm26-results.json`, Repo `Ceccaroni/wm26-tipp-updates`, MANIFEST_URL, der ewige Direktlink `WM26-Tipp-arm64.dmg`.
- **Zwei Entkopplungs-Handgriffe** (sonst hätte `productName` Name+Technik mitgezogen):
  1. **userData gepinnt** (index.ts, `else`-Zweig vor `new Store()`): `app.setPath('userData', join(app.getPath('appData'), 'WM26 Tipp'))`. `app.getPath('appData')` = `~/Library/Application Support` (namensunabhängig) ⇒ der Rename erzeugt KEINEN leeren Datenordner, echte Tipps bleiben am Platz. `appData` ist vor `app.whenReady` verfügbar (gleiches Muster wie der bestehende SHOT_DIR-Pin). Adrians echte Tipps unter `~/Library/Application Support/WM26 Tipp/` damit unberührt.
  2. **`artifactName` fix** (electron-builder.yml): `${productName}-…` → `WM26-Tipp-${version}-${arch}.${ext}`. Hält Apostroph/Leerzeichen aus dem DMG-Dateinamen und den ewigen Direktlink stabil. Mitgezogen: `publish-update.mjs` Hint-Check (`WM26 Tipp-…` → `WM26-Tipp-…`) und README (Release-Output + Empfänger-Schritte „TSCHUTTINI '26").
- **Verifiziert:** `npm run typecheck` grün (beide Projekte); Kontrollgrep zeigt nur noch beabsichtigte `wm26-*`/`.wm26tipp`/userData-Treffer. Layout-Check Onboarding-`h1`: Card-Inhalt ~396px (max-width 460 − 2×`--space-8` 32px), „TSCHUTTINI '26" in Anton 44px ≈ 295px ⇒ passt einzeilig, kein CSS-Eingriff.
- **Gebaut (auf „baue"):** Version `1.8.2 → 1.8.3` gebumpt (package.json), Changelog-Eintrag „Neuer Name: TSCHUTTINI '26" oben in `lib/changelog.ts`. `npm run dist` + `dist:intel` ⇒ `dist/WM26-Tipp-1.8.3-arm64.dmg` (151 MB) + `-x64.dmg` (158 MB), ad-hoc signiert. **Verifiziert:** Bundle heisst `TSCHUTTINI '26.app`; Info.plist `CFBundleName`/`CFBundleDisplayName` = „TSCHUTTINI '26", `CFBundleIdentifier` = `ch.lanz.wm26tipp` (unverändert), Version 1.8.3; DMG-Dateiname bleibt `WM26-Tipp-…` (artifactName-Fix greift); userData-Pin im asar-gepackten `out/main/index.js` nachgewiesen ⇒ gepackte App liest die bestehenden Tipps.
- **Veröffentlicht + verifiziert (auf Adrians „für alle neu heissen"):** `publish:update --notes "Neuer Name: …" --dmg`. Vorab geprüft: `gh` eingeloggt (Ceccaroni), Spielplan-Patch erhalten (Spiel 29 = 00:30Z, 31 = 03:00Z). Live-Manifest App **1.8.3** / Daten **v19** / Notes vollständig (npm reichte den Satz korrekt als ein Argument durch — die Echo-Zeile sah nur zerlegt aus, Manifest bestätigt den ganzen Satz). Release v1.8.3 mit 4 Assets (`WM26-Tipp-1.8.3-{arm64,x64}.dmg` + stabile `WM26-Tipp-{arm64,x64}.dmg`); ewiger arm64-Direktlink Content-Length = 158 566 941 B = lokaler 1.8.3-Build. Daten v19 inhaltsgleich zu v18 (publish erhöht die Laufnummer immer, Apps wenden idempotent an).
- **Heads-up für Mitspieler:** Nach dem Update liegt die neue `TSCHUTTINI '26.app` neben der alten `WM26 Tipp.app` (gleicher appId ⇒ geteilte Tipps dank userData-Pin); die alte kann gelöscht werden. Wer noch auf 1.1.0 ist, kennt den Update-Banner nicht (bestehender Wächter) und braucht den Direktlink einmalig manuell.

- **⮌ ZURÜCKGENOMMEN (14.06.):** Der 1.8.4-Auto-Cleanup (`migrate.ts`: alte App → Papierkorb via `shell.trashItem` + Dock-Eintrag per PlistBuddy umbiegen + `killall cfprefsd/Dock`) wurde an Adrians Mac getestet und **scheiterte**: Das Dock-Umbiegen griff auf **macOS 26 nicht** ⇒ zwei Dock-Icons, und man musste die neue App erst manuell aus den Programmen starten (schlechtes Erlebnis). Adrians Entscheid: zurück.
- **Rollback ausgeführt + verifiziert:** (1) Live-Manifest `appVersion` 1.0.0→**1.8.2**, notes entfernt (dataVersion 19 bleibt, idempotent). (2) Verunglücktes **v1.8.3-Release + Tag gelöscht** ⇒ „latest" fällt zurück auf v1.8.2; ewiger Direktlink `WM26-Tipp-arm64.dmg` = **158 599 535 B** (echter WM26-Tipp-1.8.2-Build, HTTP 200) verifiziert. (3) **Gesamter Rename-Code revertiert** (productName/Titel/Strings/PRODID/Footer/Changelog/artifactName/publish-Hint/README), userData-Pin + `migrateFromOldName`-Aufruf raus, `migrate.ts` gelöscht, `package.json` zurück auf **1.8.2**, typecheck grün. Für alle Mitspieler ist es, als wäre nie etwas passiert (sie blieben dank der Banner-Pause durchgehend auf WM26 Tipp 1.8.2).
- **Adrians Maschine** (einziger TSCHUTTINI-Installer): manuell — `TSCHUTTINI '26.app` löschen, `WM26 Tipp` wiederherstellen (aus Papierkorb „Zurücklegen" oder frisch vom Direktlink), totes/zweites Dock-Icon rauswerfen. Tipps lagen durchgehend im selben userData-Ordner, unberührt.
- **🧠 LEHRE (wichtig, bevor das je wieder angefasst wird):** Ein *sichtbarer* App-Rename auf macOS erzwingt einen neuen `.app`-Dateinamen (Finder/Dock zeigen den **Dateinamen**, nicht `CFBundleDisplayName` — empirisch getestet). Neue Datei ⇒ das alte, angeheftete Dock-Icon zeigt weiter auf die alte App ⇒ Drag-and-Drop-OTA kann das **nicht nahtlos** auflösen. Automatisches Dock-Umbiegen ist ein fragiler Hack (auf macOS 26 gescheitert). Fazit: Rename nur mit einer zuverlässigen Dock-Migration (z. B. gebündeltes `dockutil` + an mehreren macOS-Versionen verifiziert) ODER gar nicht. Die App heisst weiterhin **WM26 Tipp**.

### Live-Spiel im WM-Look: holografischer Rand ✅ (14.06., Wunsch Adrian: die App-Anzeige der WM-Broadcast-Grafik annähern, ohne Übersichtlichkeit zu verlieren — nur fürs laufende Spiel; **veröffentlicht als v1.8.2, Daten v18**)

- **Ausgangsfrage:** Adrian zeigte TV-Fotos der WM-2026-Score-Bugs (Multicolor-Neon-Rand, Cyan-Tor-Boxen, fette Versalien, Torschütze klein). Diskussion ⇒ nur die *eine* live laufende Karte aufwerten; Cyan-Boxen/Possession-Punkt/Versalien bewusst NICHT (würden Farbsemantik + Übersicht stören). Schlüssel-Einsicht: unser Holo-Foil (`foil.css`) hat exakt dieselbe Gelb/Cyan/Lila/Pink-Palette wie die WM-Grafik ⇒ wir zitieren die Optik mit **eigenem** Vokabular, kein FIFA-Klon.
- **Holografischer Multicolor-Rand auf `.liverow--live` (`styles/components.css`):** Ersetzt den roten Glow. Gradient-Border via doppeltem Background (`linear stage-raised` padding-box + `conic-gradient(from var(--live-holo-angle))` border-box, `border: 2px solid transparent`); animierter Winkel über registrierte `@property --live-holo-angle` (`inherits: true`) + `@keyframes liveholo-spin` 7 s linear. **Mittelweg (Variante C, von Adrian gewählt):** zusätzlich dezenter mehrfarbiger Schein nach außen via `::after` (gleicher conic, `blur(11px)`, `opacity .32`, `z-index -1` ⇒ hinter der Karte; erbt den animierten Winkel ⇒ synchron), Hover hebt Schein auf `.46`. **Score-Block bleibt rot** (trägt Status + Minute). `prefers-reduced-motion` ⇒ Rand statisch (zeigt trotzdem alle Farben). Gilt auf jeder Live-Karte (Heute + Live-Screen); zwei parallele Spiele ⇒ beide leuchten. `box-sizing: border-box` global ⇒ kein Layout-Shift durch 1→2 px Rand.
- **Verifiziert per Shot 7 (Live):** Mexiko 1:0 / Südkorea 0:0 mit buntem Rand + Schein, roter Score erhalten, Glow sauber hinter der Karte (Inhalt scharf); Endstand-/kommende Karten unberührt. Typecheck grün (beide Projekte). „Was ist neu"-Eintrag 1.8.2 („Live-Spiele im WM-Glanz") oben in `lib/changelog.ts`.
- **Release v1.8.2 veröffentlicht + verifiziert:** beide .dmg (arm64 158599535 B / x64 166014237 B, adhoc), `publish:update --notes … --dmg`. Live-Manifest App 1.8.2 / Daten v18 / Notes ✓; Release v1.8.2 mit 4 Assets (versioniert + stabile Direktlinks); arm64-Direktlink Content-Length = 158599535 B (= neuer Build); **Spielplan-Patch erhalten** (Spiel 29 = 00:30Z, 31 = 03:00Z; `data:schedule` nicht gelaufen). Daten v18 inhaltsgleich zu v17 — `publish:update` erhöht die Laufnummer immer (harmlos, Apps wenden idempotent an).

### Zufalls-Zitate + Intro-Sound + Tschuttini-Logo ✅ (14.06., Wunsch: Sprüche-Pool, vertontes Intro, Logo; **veröffentlicht als v1.8.1, Daten v17**)

- **34 legendäre Fußballersprüche (`lib/quotes.ts`):** `FOOTBALL_QUOTES` (Shankly, Cruyff, Herberger, Kahn, Trapattoni, Cantona, Beckenbauer, Podolski …), je `{ text, author, note? }`. `randomQuote()` → bei jedem Start einer. Intro umgebaut: hartcodiertes Shankly raus, zufälliges Zitat (`useState(randomQuote)`), Layout neu — Zitat weiß in „…", Autor in Gold (`.foil`), Kontext-Note dezent. Schriftgröße `clamp(21–31px)` + `max-width 720px`. Verifiziert per Temp-Shot mit Extremen: „Ich habe fertig!" (3 Wörter) bis Cantona-Möwen (3 Zeilen) — beide sauber.
- **Intro vertont (`intro.mp3`, von Adrian via ElevenLabs, ~20,9 s, Geige → Torjubel, eigener Ausklang):** Mix 1:1 übernommen (Pegel/RMS-Profil geprüft: Aufbau 0–8 s leise, Klimax 12–16 s, Ausklang ab 18 s — **kein zweiter Fade-out**, sonst kappt's den Jubel). `playIntro()` in `lib/sound.ts`: Fade-in 0,5 s + Stopp-Funktion (sanftes Ausblenden beim Überspringen, kein Knacken), kein Transcode. Intro-Dauer auf ~20,8 s, Bild fadet am Ende mit. **Gegen Ruckeln:** Animationen GPU-isoliert (`will-change` + eigene Compositor-Ebene), Klang-Start erst nach 2× `requestAnimationFrame` (Start-Render grätscht nicht ins Audio). Klang-Glätte nur in der echten App beurteilbar (von Adrian abgenommen).
- **Tschuttini-Logo (`assets/tschuttini.png`):** Adrians umgebautes Panini-Logo freigestellt — Hintergrund war fast einfarbig (#7ec36c), per ffmpeg `colorkey` (similarity 0,22) entfernt; die bunten Mini-Sticker (auch grüne) blieben intakt (satteres Grün ≠ Pastell-Hintergrund). Auf dunklem Grund Halo-geprüft, sauber. Sitzt als `.sidebar__brand` (img statt „WM26 TIPP"-Text), Breite 100 % der Sidebar. Platzierung von Adrian gewählt (Sidebar-Kopf statt oben-rechts — Banner zu breit/detailreich fürs Eck). Verifiziert per Shot 15.
- **Sound-Tooling:** `lib/sound.ts` um `ramp()` (volume-Fade) erweitert; `assets/sound/CREDITS.md` aktualisiert. Beide Audio-Assets via Vite gebündelt (Build-Check ok).
- **Release v1.8.1 veröffentlicht + verifiziert:** beide .dmg (arm64 159 MB / x64 166 MB), `publish:update --dmg`. Live-Manifest App 1.8.1 / Daten v17 / Notes ✓; 4 Assets; arm64-Direktlink = 158606640 B; Spielplan-Patch erhalten (29 = 00:30Z, 31 = 03:00Z). Typecheck grün, alle Temp-Shot-Patches entfernt.

### Start-Intro + Torschützenliste + Belohnungs-Moment + Sound ✅ (13.06., Wunsch: Shankly-Zitat als Intro, plus meine Vorschläge Torschützenliste + Belohnung; **veröffentlicht als v1.8.0, Daten v16**)

- **Start-Intro (`components/Intro.tsx`):** Schwarzer Vollbild-Screen, Bill Shanklys Zitat („…much, much more important than that.", Pointe als Gold-`.foil`) schwillt an (CSS-Staffel-Fades), steht ~6 s, fadet weg. Deutsche Übersetzung + „BILL SHANKLY"-Zuschreibung, dezenter „Klick zum Überspringen". **Jeder Start** (`introDone`-State, im Shot-Modus initial true ⇒ aus). Liegt mit `z-index 100` über allem; das What's-New-Overlay erscheint erst nach dem Intro (`{introDone && <WhatsNew/>}`). App.tsx-Return auf Fragment umgebaut. Verifiziert per Temp-Shot (introDone temporär false + Intro-Screenshot, danach zurückgesetzt).
- **Torschützenliste (`screens/Torschuetzen.tsx` + `lib/scorers.ts`):** Neue Rubrik (Sidebar-**Index 10**, vor Profile; Hook-Indizes 0–9 unberührt). `buildScorers()` aggregiert Tore (inkl. Elfmeter, **ohne Eigentore** — offizielle Zählung) je Spieler über alle Results, Identität via Kader-Karte (`findSquadPlayer`), Sortierung Tore ↓. Goldener Schuh 👟 + Gold an der Spitze, Foto/Initialen + Flagge + Position, Klick → PlayerPeek. Hook um Shot 22 erweitert. Verifiziert per Shot (Larin Doppelpack als Spitze, Aggregation + Matching korrekt).
- **Belohnungs-Moment (`components/RewardMoment.tsx`):** Gold-Konfetti (deterministische CSS-Bahnen) + Karte „🎯 Volltreffer! · exakt getippt · +N", wenn ein Spiel endet und das **aktive** Profil es exakt getippt hat. Beobachtet Results wie der Tor-Alarm: erster Durchlauf = Baseline (kein Nachhol-Gewitter), danach jeder neue Volltreffer einmal. `pointer-events: none` (blockiert die App nicht), Auto-Dismiss nach 4 s. Im Shot-Modus aus. Verifiziert per Temp-Shot (Demo-Trigger, danach entfernt).
- **Sound (`lib/sound.ts`):** `cheer.ogg` (**Public Domain**, Wikimedia Commons „Applause i.ogg" von thore — Lizenz in `assets/sound/CREDITS.md`), via Vite gebündelt. `playCheer()` beim Volltreffer (Lautstärke 0.55, sanfter Abbruch nach 2.6 s). Globaler Stummschalter in **Profile** (`SoundToggle`, localStorage `wm26-muted`, Default an); Einschalten spielt eine **Kostprobe**. **Intro bleibt für 1.8 bewusst tonlos** (kein wasserdicht-CC0-Geigen-Swell gefunden — Freesound bräuchte Login, orchestrale Aufnahmen sind geschützt). Jubel-Sample ist ein Platzhalter (Publikums-Applaus, kein Stadion-Roar) — Tausch = Datei in `assets/sound/` ersetzen.
- **Nebenbefund:** Die Spacing-Tokens **`--space-5` und `--space-7` existieren NICHT** (Set: 1/2/3/4/6/8) — beim Intro/What's-New-CSS zunächst genutzt, dann auf `--space-6`/`--space-8` korrigiert (sonst fallen die Paddings still aus, wie schon bei `--space-5` in der Chronik-Session).
- **Shot-Seed erweitert:** Spiel 3 (Larin #9 ×2, Lukic #25) + Spiel 7 (Neymar #10, Vinícius #7) bekamen score-konsistente Tor-Events mit echten Kaderspielern — füllt Torschützenliste + Chronik-Torschützen in den Screenshots.
- **Release v1.8.0 veröffentlicht + verifiziert:** beide .dmg (arm64 158 MB / x64 165 MB, adhoc), `publish:update --dmg`. Live-Manifest App 1.8.0 / Daten v16 / Notes ✓; 4 Assets (versioniert + stabile Direktlinks); arm64-Direktlink HTTP 200 = 157955900 B; **Spielplan-Patch erhalten** (Spiel 29 = 00:30Z, 31 = 03:00Z; `data:schedule` nicht gelaufen). Typecheck grün, alle Temp-Shot-Patches entfernt.
- **OFFEN (mit Adrian, „heute nach dem Spiel"):** Intro-Geige + besserer Jubel-Sound „richtig" machen → dann ggf. 1.8.1.

### Chronik-Tipp-Vergleich + Torschützen-Backfill + „Was ist neu" ✅ (13.06., Wunsch: fehlende Torschützen der ersten Spiele, Tipp-vs-real in der Chronik, Update-Overlay; **veröffentlicht als v1.7.0, Daten v15**)

- **Torschützen-Nachtrag (selbstheilend, `src/main/results.ts`):** Wurzel gefunden — Spiel 1 (MEX 2:0 RSA) + 2 (KOR 2:1 CZE) standen mit korrektem Endstand, aber 0 Events im echten Store (vor Einführung des Event-Parsings erfasst). Zwei Bugs: (1) `sameResult()` verglich die `events` NICHT → ein finished-Spiel mit stimmendem Score wurde nie wieder angefasst, auch wenn ESPN die Torschützen nachreicht; (2) `eventMap` voll (104/104) ⇒ ESPN-Fenster nur noch ±36 h ⇒ alte Spiele wurden nie neu geholt. Fix: `sameResult` um `sameEvents()` erweitert; neue `hasGoalGap()` (beendetes Spiel mit Tor-Events < Toren); Fenster-Logik in `pollEspn` = solange `!eventsComplete` von Turnierstart bis heute+36 h abfragen, sonst schmal. Deckt Adrian UND alle Mittipper ab (kein gebündeltes Daten-Backfill, kein manuelles Patchen des Stores nötig). **Greift beim nächsten App-Start mit dem neuen Code** (mit altem Code zieht ein laufender Client nichts nach). **Verifiziert:** `espnToLiveResult` gegen die echten ESPN-Rohdaten vom 11./12.06. (esbuild-Wegwerftest) — Spiel 1 → 2 Tore (Quiñones 9', Jiménez 67') + 3 Rote, Spiel 2 → KOR 2:1 mit korrekter Heim/Auswärts-Zuordnung; Tor-Events == Tore. ESPN liefert die `details` für beide Spiele weiterhin vollständig.
- **Tipp-Vergleich in der Chronik (alle Tipper pro Spiel — von Adrian gewählt):** `lib/chronik.ts` → `ChronikDay.matchTips: Record<number, ChronikTip[]>` je beendetem Spiel, in `buildChronik` aus den bereits berechneten Profil-Breakdowns gefüllt (`scoreMatch`/`baseKind` für Basis, `bd.perMatch[].advance` für KO-Bonus). Sortierung: Spielpunkte ↓, dann exakt, dann Getippte vor Nicht-Getippten, dann Name. `screens/Chronik.tsx` → neue `MatchTips`-Komponente unter den Torschützen jedes beendeten Spiels: Dot + Name + Tipp + Punkte; beste Punktzahl in Gold, „exakt"-Label, aktives Profil blau hinterlegt + fett, nicht getippt = „– –". Läuft auch im PDF-Export (DayPage bekommt `activeId`). CSS `.chroniktips*` neben den Tagespunkte-Chips. **Verifiziert per Shot:** Tag 3/Spiel 4 (Reto 1:3 +4 exakt, Shot-Test 1:1 0, Livia – –) und Tag 2/Spiel 3 (Shot-Test 2:1 +4 exakt, Reto 1:0 **+3** = richtige Tordifferenz, Livia 0:1 0); laufendes Spiel zeigt korrekt keine Tabelle.
- **„Was ist neu"-Overlay nach Update (`components/WhatsNew.tsx` + `lib/changelog.ts`):** Gebündelter Changelog (1.4.0/1.5.0/1.6.0/1.7.0, Nutzersprache), zuletzt gesehene Version in `localStorage['wm26-last-seen-version']`. Trigger beim Start: zeigt alle Einträge mit Version > gesehen ∧ ≤ laufend; **Erststart/frische Installation (nichts gemerkt) zeigt nichts**, vermerkt nur die Version (kein Changelog für Neulinge; greift ab dem nächsten echten Sprung). Schließen via Button/Escape/Backdrop schreibt die laufende Version. Panini-Albumkarte: Anton-Gold-Titel (`.foil`), blaue Versions-Badges, ⚽-Bullets, peekfade-Backdrop, `z-index 90`. In `App.tsx` nur im Hauptscreen gerendert (nach Onboarding). **Verifiziert per Temp-Shot:** localStorage auf 1.5.0 geseedet + reload → Overlay zeigt 1.7.0 + 1.6.0 korrekt.
- **Verifikation gesamt:** Typecheck grün (beide Projekte), kompletter Shot-Hook lief 3× durch (21 Screens + PDF). Alle Temp-Shot-Patches in `src/main/index.ts` (Tag-2-Klick, What's-New-Reload) wieder entfernt — keine Reste.
- **Release v1.7.0 veröffentlicht (13.06.):** `npm run dist` + `dist:intel` (beide .dmg adhoc signiert, arm64 157 MB / x64 165 MB, Identifier ch.lanz.wm26tipp), dann `publish:update --notes … --dmg`. **Verifiziert:** Live-Manifest = App 1.7.0 / Daten v15 / Notes gerendert; Release v1.7.0 mit 4 Assets (versioniert + stabile `WM26-Tipp-{arm64,x64}.dmg`); stabiler Direktlink liefert die neue arm64-.dmg (HTTP 200, 157332526 B); **Daten-Release behält den verteilten Spielplan-Patch (Spiel 29 = 00:30Z, 31 = 03:00Z)** — `data:schedule` bewusst NICHT gelaufen. Alte 1.6.0-.dmg aus `dist/` entfernt. Mittipper sehen den Update-Banner beim nächsten Poll (≤ 6 h); der Torschützen-Fix greift bei jedem Client nach der Installation/dem Neustart.

### M7 ✅ — Turnier-Chronik mit PDF-Export (12.06. spätnachts, erster der drei abgenommenen Feature-Vorschläge; **veröffentlicht als v1.6.0, Daten v14**)

- **Neue Rubrik „Chronik"** (Sidebar-Index 9, hinter „Erfolge" — Hook-Indizes 0–8 unverändert; Hook neu: Shot 21 + `chronik.pdf`): pro Turniertag eine Album-Seite, rückwirkend aus den persistierten Results/Events gebaut, nichts persistiert (Philosophie wie lib/achievements).
- **`lib/chronik.ts → buildChronik()`:** Tage bis heute (der heutige erst ab dem ersten Endstand). „Stand nach Tag X" wertet NUR Ergebnisse ≤ Tag X (computeBreakdown je Profil gegen den Tages-Snapshot) — Durchtipp-Boni erscheinen so an dem Tag, an dem die Runde in echt feststand; **Tagespunkte = Differenz der Gesamtstände** (fängt auch nachträglich fällige Boni); Wettkampf-Ränge wie in der Rangliste (Tiebreak exakte Treffer, dann Name); Tippbäume einmal pro Profil aufgelöst.
- **`screens/Chronik.tsx`:** Tag-Chips (Gold-Rand = Tag komplett), Albumseite mit Spielzeilen (Sieger gold, laufend rot „läuft", ausstehend gedimmt mit Anstoßzeit), Torschützen-/Rot-Chips — Klick öffnet die Panini-Karte (PlayerPeek/findSquadPlayer, per Temp-Shot inkl. Foto verifiziert; ein erster Capture ohne Foto war ein Decode-Race der Screenshot-Automatik, kein Bug), Tagespunkte-Chips (Tagessieger 👑 + Gold), „Stand nach Tag n" mit Bewegungspfeilen ▲/▼ (Movement rendert IMMER eine Zelle — fehlende Zelle verschob am ersten Tag die Grid-Spalten).
- **PDF-Export „Als PDF sichern":** zweiphasig — Renderer rendert Print-Modus (Deckblatt mit Emblem/Tippernamen/Stand + ALLE Tagesseiten in `.chronikprint`), dann IPC `chronik:pdf` → `printToPDF` A4 mit printBackground, Save-Dialog; im Shot-Modus dialogfrei nach `$WM26_SHOT_DIR/chronik.pdf` (der Hook prüft das PDF mit). Print-CSS-Fallen gelöst: Höhen/Overflow aufheben (sonst schneidet `.screen` den Inhalt auf Viewporthöhe ab), `html`+`body` weiß UND `win.setBackgroundColor('#ffffff')` während des Drucks (die dunkle Fenster-Hintergrundfarbe schlug sonst als schwarzer Seitenrand durch — danach zurückgesetzt), Albumseite `min-height: 92vh` (füllt das A4-Blatt), Teamnamen brechen im schmaleren A4 um statt Ellipsis („Bosnien-Herzegowina"), eine Seite pro Tag (`break-after: page`).
- **Nebenbefund gefixt:** `--space-5` existiert nicht im Token-Set → `gap` in `.liverow__events` fiel komplett aus (Torschützen-Spalten klebten zusammen); auf `--space-6` korrigiert (Live + Chronik), Shot 7 gegengeprüft.
- **Verifikation:** Typecheck grün, test:scoring 22 ✓, test:news 41 ✓, kompletter Hook (21 Shots + PDF), Shots 7/15/19 unverändert bzw. verbessert; PDF visuell geprüft (Deckblatt, Tag 1+2, Umbrüche, kein schwarzer Rahmen). **Release verifiziert:** codesign ok (adhoc), Chronik-Strings im app.asar (Main/Preload/Renderer), Manifest live (1.6.0 / Daten v14, Notes gerendert), stabiler Direktlink liefert die neue arm64-.dmg, alte Artefakte aus `dist/` entfernt.

### Startsequenz-Umbau ✅ (12.06. spätnachts, Feedback: „Startroutine dauert zu lange, verbraucht zu viele Tokens")

- **STATUS.md** (war auf ~26k Tokens angewachsen) → kompakte Übergabe, hart ~60 Zeilen: Stand / ⚠️ Wächter / Nächstes. Komplettes Arbeitslog hierhin verschoben (**HISTORIE.md**, append-only, neueste oben).
- **CLAUDE.md:** Doku-Tabelle + Regeln angepasst — Sessionstart = NUR STATUS.md lesen; Sessionende = erledigten Block nach HISTORIE.md, STATUS.md neu schreiben statt anwachsen lassen. **README:** OTA-/Release-Workflow (`publish:update`) dokumentiert.
- Memory `user-arbeitsstil` um die Token-Budget-Regel erweitert. Startsequenz jetzt ~2k statt ~27k Tokens.

## Stand: 12. Juni 2026, nachts

**Kurzfassung: Aktuelle Version 1.5.2 (Daten v13), alles live. Die App ist der komplette WM-Sofa-Begleiter: Heute-Dashboard (mit WM-26-Emblem), Live-Spiele mit Torschützen/Platzverweisen — Klick öffnet die Panini-Karte des Spielers —, „Stand jetzt"-Tabellen/-Baum, Tor-Alarm (opt-in, Spoiler-Warnung), Tipp-Wächter, Tipp-Vergleich ab Anstoß, Erfolgs-Sticker-Heft (11 Auszeichnungen), interne Team-News (9 Feeds inkl. Volltext-Spielberichte, Junk-Filter), Team-Dossiers mit allen 48 Verbandswappen (19 Commons-PD + 29 privat beschafft), 1248 Kaderspieler als drehbare Panini-Karten (982 mit Foto), drehbare Stadion-Karten (Flips seit 1.5.1 ruckelfrei), Doppel-Rangliste mit Gold-Holo auf Platz 1. Updates OVER THE AIR über github.com/Ceccaroni/wm26-tipp-updates; „Herunterladen & schließen" beendet die App fürs Installieren selbst. Offen: Mittipper von 1.1.0 wegholen (einmalig, danach Banner), Cloud-Routine liefert TÄGLICH 09:00 MESZ den Nati-Foto-Bericht (Funde lokal einpflegen → braucht Code-Release, Fotos sind im Bundle), `npm run data:schedule` NICHT laufen lassen, solange fixturedownload Spiel 29/31 noch mit alten Zeiten führt (unser Patch ist verteilt), am 28.06. tippen alle die KO-Kategorie (App erinnert selbst).**

### Heute-Feinschliff ✅ — beendete Spiele verschwinden (12.06. nachts, **veröffentlicht als v1.5.2, Daten v13**)

- **Feedback:** „Wenn ein Spiel vorbei ist, soll es aus ‹Heute› verschwinden — der User will sehen, was JETZT ansteht."
- **Neu in `screens/Heute.tsx`:** „Heutige Spiele" zeigt nur noch Spiele mit `status !== 'finished'` (laufende + angesetzte). Beendete: dezenter Verweis rechts im Tageskopf „✓ n beendet — Ergebnisse →" (klickbar → Live-Rubrik, `goTo` um `'live'` erweitert; Style `.dayhead__done` in components.css). Zähler heißt dann „noch n Spiele". Sind ALLE heutigen Spiele durch: Hinweis „Für heute ist alles gespielt — die Ergebnisse stehen in der Live-Rubrik" (HUD zeigt dann ohnehin den Countdown zum nächsten Anstoß). Live-Rubrik unverändert (dort bleiben alle Ergebnisse).
- **Verifikation:** Typecheck grün, Shot 15 (nur noch KOR–CZE/Halbzeit sichtbar, „✓ 1 beendet"-Link, Vergleich klappt weiter auf) + Temp-Shot mit allen heutigen Spielen beendet (Notice + Countdown) visuell geprüft, Temp-Seed zurückgebaut. Shot-15-Klick auf `.liverow--expandable` traf schon vorher die KOR-CZE-Zeile (zeitlich erste) — Hook unverändert.
- **Release verifiziert:** codesign ok (adhoc), Feature-Strings im app.asar nachgewiesen, Manifest live (1.5.2 / Daten v13, Notes gerendert), stabiler Direkt-Download-Link liefert die neue arm64-.dmg (157 MB). Alle ab v1.4.0 sehen den Banner beim nächsten Poll (max. 6 h).

### Session-Abschluss 12.06. nachts

- **Nati-Foto-Routine auf TÄGLICH umgestellt** (war: einmalig Mo 15.06.): Cron `0 7 * * *` UTC = 09:00 MESZ, Routine `trig_01BmaXJkHosF4RuLTqzXogoe` auf claude.ai/code/routines, nächster Lauf 13.06. Bericht kommt auf claude.ai; Funde lokal einpflegen (Fotos stecken im App-Bundle → Code-Release nötig). Löschen, wenn alle 8 gefunden — geht nur über die Routinen-Seite.
- **Claude ist als Mittipper importiert** (Screenshot-Beleg: 6 Profile, Claude „101/72 getippt · importiert"). Spielregeln + Pflege-Pipeline sind im persistenten Memory verankert (`projekt-claude-tippt-mit`), damit Folgesessions informiert sind und die Tipps künftiger Spiele nachschärfen dürfen („rumzündeln" ausdrücklich erwünscht).
- **Nächste Session beginnt vereinbart mit 3 Feature-Vorschlägen** (im Memory `projekt-feature-ideen`): Turnier-Chronik als Sammelalbum (zuerst — baut Tage rückwirkend auf), Torschützenliste aus den MatchEvents, Siegerehrungs-Screen.

### Claude tippt mit ✅ (12.06. nachts, Wunsch: „kannst ne Datei zum Importieren anlegen?" + „tritt an, um zu gewinnen!")

- **`~/Desktop/Claude.wm26tipp`** — Profil „Claude" (id `claude-fable`, Farbe #D97757), 101 Tipps (Spiele 4–104), Import über die Profile-Rubrik. Spielregeln: Spiele 1–3 bewusst LEER (waren beim Tippen schon angepfiffen/vorbei — kein Tippen auf Bekanntes; alle Tipps vor deren Anstoß fixiert, exportedAt 12.06. 21:30 MESZ).
- **V2 recherche-kalibriert statt Bauch** (Adrians Wunsch, 3 parallele Web-Agenten): Tendenzen nach DraftKings-1X2-Quoten ALLER restlichen Gruppenspiele (via ESPN-Scoreboard-API, 12.06. ~21:25), Scorelines nach wahrscheinlichstem Resultat; eingepreist: BRA-Ausfälle (Neymar/Rodrygo/Militão/Estêvão zum Auftakt), NED-Ausfälle (Timber/Simons/Schouten/de Ligt), MEX ohne Montes (Rot) vs KOR, RSA-Doppelsperre (Sithole/Zwane); Turnier-Spine nach Markt+Modellen (FanDuel 12.06.: ESP 5.5 vor FRA 6.0; Opta 16,1 % ESP; Goldman 26 % ESP). Quellen-Details in den Agenten-Berichten der Session.
- **Erzeugung über `scripts/claude-tips.ts`** (bewusst behalten): Gruppentipps → `resolveTipBracket` (echter App-Code) → KO rundenweise gegen die eigene Auflösung getippt. Weil Spiele 1–3 fehlen, bleiben Gruppe A/B in der Tipp-Welt unauflösbar → betroffene KO-Äste „blind" auf die Spielnummer getippt (KO-Basis ist teamunabhängig, §11.6; Durchtipp-Boni über diese Äste entgehen Claude — fairer Nachteil des Späteinstiegs).
- **Claudes Baum (V2):** SUI gewinnt Gruppe B, 1/8-Final-Aus 1:2 gegen POR; Halbfinals FRA–ESP und ENG–ARG, **Finale ESP–ARG 2:1**, FRA Dritter. Spiel 8: QAT–SUI 0:2.
- **Verifiziert:** Import end-to-end gegen die echte `Store`-Klasse (isoliertes userData), inkl. Re-Import-Fall: neueres exportedAt überschreibt einen bereits importierten älteren Claude-Stand korrekt; `computeBreakdown` läuft fehlerfrei.
- **Idee für den 28.06.:** aktualisierte Datei mit `fromR32`-Kategorie nachliefern (neueres exportedAt gewinnt beim Re-Import).

### Nebenbefund 12.06. nachts

- fixturedownload führt Spiel 29/31 **weiterhin** mit den alten Zeiten (01:00Z/04:00Z) — `npm run data:schedule` bleibt gesperrt, unser schedule.json-Patch gilt.

### M6.1 ✅ — Verbandswappen + WM-Emblem + Flip-Fix (12.06. abends, **veröffentlicht als v1.5.1, Daten v12**)

- **Wappen-Sticker auf der Team-Detailseite** (rechts oben, „eingeklebt": weißer Rand, 3,5° gedreht): `components/Crest.tsx` + `assets/crests/`. **19/48 echte Verbandslogos** von Commons (3 Agenten, nur PD/CC0 — CC-BY-User-Dateien offizieller Logos sind Falschlizenzierungen und wurden strikt verworfen; „trademarked"-Restriction ok, da privater Gebrauch, Hinweis im Tooltip): MEX USA BRA PAR CAN · GER ECU JPN ESP · ARG AUT ALG COL CRO GHA IRQ JOR NOR UZB. `credits.json` deckungsgleich validiert.
- **29 ohne freie Lizenz** (u. a. SUI — SFV-Logo existiert nicht auf Commons; ENG/FRA/POR/NED/KOR… nur non-free auf enwiki): **eigenes Fallback-Wappen** — Schildform (clip-path) mit Teamflagge + Trigramm-Band, visuell geprüft. **Update 12.06. abends: Adrian hat die 29 selbst beschafft** (Entscheid: Eigengebrauch Art. 19 URG, Verantwortung übernommen) → `~/Desktop/wappen/` gesichtet, zugeordnet (Achtung: 11 „.webp" waren SVGs, Haitis „.svg" ein PNG — per `file` erkannt), normalisiert (PNG max 400 px bzw. SVG direkt) → **alle 48 Teams haben jetzt ihr echtes Wappen** (3,5 MB gesamt). Crest-Tooltip unterscheidet: Commons-Funde mit Lizenz, private ohne. Fallback-Schild bleibt als Sicherheitsnetz im Code. Verifiziert: KOR/CIV-Stichproben, SUI (echtes SFV-Logo) im Temp-Shot.
- **WM-„26"-Emblem im Heute-Titel:** `assets/wm26-emblem.svg` = Commons „2026 FIFA World Cup emblem (without trophy).svg", **Public domain** (keine Schöpfungshöhe; Marke der FIFA — Tooltip-Hinweis). Die Vollversion MIT Pokal ist auf Commons als CC BY-SA eines Users gelistet = Falschlizenzierung → bewusst nicht verwendet. Dunkle Bühne: per `filter: invert(1)`.
- **Rechtsrahmen dokumentiert:** Markenrecht greift nur im geschäftlichen Verkehr; CH-Eigengebrauch (Art. 19 URG, enger Kreis) deckt die private Verteilung. Projektlinie bleibt: nur PD/CC0-Dateien, Attribution im Tooltip.
- **Flip-Ruckler behoben (Feedback: „2 von 3 hängen kurz in der Animation"):** Ursache war NICHT Bildgröße, sondern Erst-Flip-Jank: `.flipcard__inner` bekam den Compositing-Layer erst beim Klick, und die Rückseite (backface-hidden) wurde vor dem ersten Flip nie gemalt → JPEG-Dekodierung+Rasterung synchron am Animationsstart. Fix: `will-change: transform` auf `__inner`, `translateZ(0)` auf `__back` (Rückseite wird beim Mount gerastert), `decoding="async"` auf Stadion-/Spielerfotos. Zweiter Flip war schon immer smooth (Cache) — deckt sich mit dem „2 von 3"-Symptom.
- **Verifikation:** Typecheck + alle Tests grün, neuer Regressions-Shot 20 (Deutschland-Detail mit echtem DFB-Wappen), SUI-Fallback + Heute-Emblem per Zusatz-Shots in Originalauflösung geprüft (Temp-Shot danach zurückgebaut), Shots 14/17 nach dem Flip-Fix unverändert.

### M6 ✅ — Sofa-Begleiter-Paket (12.06. nachmittags, Wunsch „mach das alles!" nach Werkschau; **veröffentlicht als v1.5.0, Daten v11** — inkl. Spielplan-Fix 29/31 und „Herunterladen & schließen")

- **Live-Ereignisse (Tore + Platzverweise):** `LiveResult.events` (neuer Typ `MatchEvent`), geparst aus ESPN-Scoreboard-`details` in `espnToLiveResult()` — Tore (inkl. Elfmeter `(P)`/Eigentor `(ET)`-Markierung), Rote Karten; Gelbe Karten und Elfmeterschießen-Schüsse bewusst nicht (Rauschen; i.E. bildet `pens` ab). Eigentore ordnet ESPN dem profitierenden Team zu (scoreValue zählt für team.id). Anzeige in der LiveRow als zweispaltige Ereignis-Zeile (Heim/Auswärts). **Gegen die echten Spiele 1+2 verifiziert** (3 Platzverweise MEX–RSA, 3 Tore KOR–CZE, Seiten korrekt).
- **Panini-Brücke 🃏:** Klick auf einen Torschützen öffnet seine Kader-Karte als Overlay (`components/PlayerPeek.tsx`, gleiche playercard-Optik + HoloSticker). Matching ESPN-Name → Kader in `lib/players.ts → findSquadPlayer()`: Token-Mengen-Vergleich (reihenfolgeunabhängig — „Hwang In-Beom" = „In-beom Hwang"), Fallback Trikotnummer (= offizielle WM-Kadernummer), Fallback eindeutiger Nachname. Gemeinsame Bausteine (PLAYER_PHOTOS/CREDITS, POS_LABEL, initials) aus Teams.tsx nach `lib/players.ts` extrahiert. Visuell verifiziert (Shot 19: Quiñones-Karte nach Klick).
- **„Stand jetzt"-Projektion:** `resultsAsTips(results, includeLive)` — Live-Tabellen und Live-Baum rechnen laufende Spiele mit aktuellem Stand mit (Default an, Umschalter „⚡ Stand jetzt / Nur beendete" erscheint nur bei laufenden Spielen). **Wertung bleibt unberührt** (breakdown nutzt weiter nur Endstände). Shot 8: HT-0:0 und Live-1:0 zählen in den Gruppentabellen.
- **Tor-Alarm 🔔 (opt-in):** `useGoalAlarm()` in App.tsx vergleicht bei jedem Results-Push die Tore laufender Spiele (Baseline pro Spiel, VAR-Rücknahmen still, Baselines laufen auch bei Alarm aus — kein Nachhol-Gewitter). Mitteilung „⚽ TOR — Südkorea 2:1 Tschechien" + Torschütze aus den Events. **Default AUS mit Spoiler-Warnung im Tooltip** (Feeds sind dem TV-Bild 30–60 s voraus!); Schalter im HUD von Live + Heute, persistiert in localStorage (`wm26-goal-alarm`).
- **Erfolgs-Sticker (neue Rubrik „Erfolge", Sidebar-Index 8 — Hook-Indizes 0–7 unverändert!):** 11 Erfolge in `lib/achievements.ts`, rein deterministisch aus Tipps+Ergebnissen (nichts persistiert): Gruppenheft/Volles Heft, 1/5/10× exakt, 10× gepunktet, Perfekter Tag (alle Spiele eines Tages ≥3 mit Punkten), 8 Weiterkommer, 8 Achtelfinalisten, Finalist, Weltmeister. Verdiente = Gold-Foil-HoloSticker, offene = gestrichelte Lücken mit Fortschrittsbalken (Panini: fehlende Bilder!).
- **Spiel 29/31 gefixt:** ESPN-Verschiebung gegen offizielle Quellen verifiziert (Stadt Philadelphia: BRA–HAI 20:30 ET = 00:30Z; Levi's Stadium: TUR–PAR 20:00 PT = 03:00Z) → `schedule.json` direkt gepatcht. **Achtung: `npm run data:schedule` NICHT laufen lassen, solange fixturedownload die alten Zeiten führt** (Stand 12.06. noch 01:00Z/04:00Z) — würde den Patch überschreiben. Geht mit dem nächsten Daten-Release an alle.
- **Nati-Fotos nachrecherchiert (Agent, 65 Quellen-Checks):** 1 von 9 gefunden — **Zeki Amdouni** (Frame aus CC-BY-3.0-Video des Kanals ZACK, Identität über Namenseinblendung verifiziert, visuell geprüft) → **982/1248 Fotos, Nati 18/26**. Die übrigen 8 (Manzambi, Vargas, Aebischer, Keller, Rieder, Amenda, Jaquez, Itten) haben nachweislich kein frei lizenziertes Einzelfoto (Commons + Wikidata P18/P180 + Vereins-Kategorien geprüft; auch de-/en-Wikipedia ohne Artikelbild; YB-Teamfoto verworfen — Einzelpersonen nicht sicher identifizierbar). Dafür läuft eine **einmalige Cloud-Routine am Mo 15.06., 09:00 MESZ** („WM26: Nati-Spielerfotos auf Commons nachsuchen", claude.ai/code/routines — recherchiert nur und liefert einen Fund-Bericht mit Commons-Dateinamen/Lizenzen; Einpflegen danach lokal mit dem Bericht).
- **Verifikation:** Typecheck grün, test:scoring 22 ✓, test:news 41 ✓, Event-Parsing gegen echte ESPN-Daten (eigenes Testskript), Shots 7/8/15/18/19 visuell geprüft (Ereignis-Chips, Stand-jetzt-Tabellen, Tor-Alarm-Toggle, Sticker-Heft, PlayerPeek).

### Update-Banner: „Herunterladen & schließen" ✅ (12.06. mittags, veröffentlicht als Teil von v1.5.0)

- **Feedback:** Beim Installieren des Updates stört die laufende App (Ersetzen im Programme-Ordner scheitert) — Adrian „fällt immer wieder drauf rein".
- **Neu:** Banner-Button heißt „Herunterladen & schließen" — neuer IPC `update:download`: Main öffnet die Download-URL **aus dem eigenen Manifest** (nicht vom Renderer) im Browser und beendet die App 800 ms später (`app.quit()`; `before-quit` flusht den Store). Hint-Text im Banner entsprechend angepasst.
- **Verifiziert end-to-end:** Dev-Lauf mit Version 1.4.6 + temporärem Auto-Klick + URL-Override (example.org statt 155-MB-dmg): Banner erschien, IPC kam an, Browser öffnete sich, App beendete sich selbst. Test-Hacks danach entfernt, Typecheck grün.

### M4.1 v3 ✅ — News-Pflege (12.06. vormittags, Feedback: sport.de-Video-Schnipsel bei Südkorea; veröffentlicht als v1.4.7, Daten v10)

- **Junk-Filter `isJunk()` in `shared/news.ts`:** Items, deren Inhalt nur im verlinkten Video/Audio/Ticker/TV-Programm steckt, fliegen vor der Aggregation raus. Muster-Inventar aller Feeds per Agent erhoben (12.06.): kicker `…/video`-Links, Sportschau „- die Highlights/Tore"/Podcasts/ARD-Mediathek/-Sounds, NZZ LIVE-TICKER/PODCAST/BILDSTRECKE-Präfixe, Dauer-Ticker (liveblog/liveticker/tagesticker, Blick `-im-ticker-`), TV-Hinweise („Hier läuft", „Übertragung … live im TV", „… LIVE"), Eigenwerbung (Newsletter/Wunschelf), leere/SDA-Stubs (Text < 40 Z. oder = Titel). Bewusst quellenspezifisch verankert (URL-Slugs, Titel-Anker, z. T. case-sensitiv) — freie Worttreffer wie „Video"/„live" träfen echte Artikel darüber. Live-Bilanz: ~59 von ~440 Roh-Items gefiltert.
- **Schriftliche Spielberichte:** 2 neue Volltext-Feeds per Agenten-Recherche (40+ URLs geprüft): **stern.de Sport** (dpa-Volltexte in content:encoded, Median 2785 Z., Berichte zu beiden bisherigen Spielen, 0 Junk) und **t-online WM-Feed** (`feeds.t-online.de/rss/fussball-wm`, Volltext, 100 % WM). Volltext-Quellen stehen jetzt VORNE in FEEDS — bei Titel-Dubletten gewinnt im Dedupe die Version mit Text. dpa berichtet jedes WM-Spiel → solide Abdeckung.
- **Parser:** Text = längstes brauchbares Feld (statt fixer Priorität); Watson-Verweis-Stub „(Auf watson.ch lesen)" zählt nicht mehr (description-Teaser gewinnt — vorher 19/70 Watson-Items faktisch leer); Sportschau-„[ mehr ]"-Suffix gestrippt.
- **Blick-403-Fix:** Akamai blockt seit 12.06. auch unseren UA — `NewsFeed.userAgent` pro Feed, Blick läuft mit `curl/8.6.0` (live verifiziert; ohne Fix fiel Blick still aus).
- **Team-Matching geschärft** (Feedback „spuckt Komisches aus"): Titel-Treffer verlässlich, Text-Treffer nur ergänzend solange gesamt ≤ 3 Teams — Spielbericht KOR–CZE landet nicht mehr via Gruppenkontext bei MEX/RSA, Rundschau-Artikel („Das war die WM-Nacht" ohne Team im Titel) bleiben teamlos.
- **MAX_ITEMS 250 → 300** (mit 9 Feeds wurde sonst gekappt).
- **Verifikation:** `test:news` auf 41 Assertions ausgebaut (alle Junk-Muster + False-Positive-Guards: INTERVIEW-Präfix, Artikel ÜBER Videos/TV, kleingeschriebenes „live"), Live-Lauf aller 9 Feeds: 250+ Meldungen, KOR-Liste sauber (Volltext-Spielberichte statt Video-Schnipsel), Typecheck + test:scoring grün.

### Kosmetik Rangliste ✅ (12.06. morgens, veröffentlicht als v1.4.6, Daten v9)

- **Formkurve nutzt die Kachelbreite:** `.spark__col` verteilt sich per `flex: 1` über die volle Detailbreite, Balken füllen die Spalte (`width: 100%`, gedeckelt auf 64 px gegen Klotz-Balken bei wenigen Spalten).
- **Platz 1 schimmert beim Hovern:** Die führende Zeile (gleiche Bedingung wie `lbrow--first`) ist jetzt in `HoloSticker gold` gewickelt — Gold-Foil-Sweep + Glare wie beim Weltmeister-Banner. Neuer Wrapper-Style `.lbholo` (Karten-Radius; Gold-Glow liegt auf dem Wrapper, weil `overflow: hidden` des Foil-Layers den Button-Schatten kappen würde, beim Hovern intensiver).
- Verifiziert: Typecheck grün, Sichtprüfung per temporärem Zusatz-Shot (Balkenverteilung + simulierter Hover-Foil, Hook danach zurückgebaut).

### M4.3 ✅ — WM-Begleiter-Paket (11.06. nachmittags)

- **Tipp-Vergleich „Was hat Reto getippt?" (BRIEFING §3.8 vervollständigt):** `lib/compare.ts → useCompare()` (Breakdowns aller Profile memoisiert, gleiche Maschinerie wie Rangliste) + `LiveRow` nach `components/LiveRow.tsx` extrahiert und aufklappbar gemacht (Chevron, Klick). Vergleichszeile pro Profil: Tipp, „Wer kommt weiter"-Pick, Punkte-Pill (Basis + Weiterkommer-Bonus, gold bei exakt). **Fremde Tipps erst ab Anstoß sichtbar** — vorher „getippt ✓ — sichtbar ab Anstoß" (Anti-Abschreib-Regel, eigener Tipp immer sichtbar). Aktiv in Live und Heute.
- **Tipp-Wächter:** `lib/watch.ts → openTipMatches()` (heutige, noch tippbare, ungetippte Spiele; KO nur wenn die eigene Kette beide Teams auflöst) + `useTipWatch()` in App.tsx — roter Badge an „Meine Tipps", **Dock-Badge** via IPC `badge:set` (macOS), **Mitteilung 30 Min vor Anstoß** („In 28 Min: … — noch kein Tipp!", einmal pro Spiel und Sitzung, Klick fokussiert die App). Im Shot-Modus deaktiviert (neues Preload-Flag `shotMode`).
- **„Heute im Album"-Startscreen** (neue erste Rubrik, Default beim Öffnen): Begrüßung mit Datum, GlassHUD mit Live-Zähler bzw. **Countdown zum nächsten Anstoß** (30-s-Ticker) + Punktestand, roter „X Spiele noch nicht getippt"-Alert mit Sprung zu Meine Tipps, heutige Spiele als LiveRows (mit Vergleich), Morgen-Vorschau als MatchCards.
- **Kür · Formkurve:** Rangliste-Detail zeigt statt Chips ein Mini-Balken-Chart (Punkte je Spieltag/KO-Runde, Tooltip mit Langname).
- **Kür · Bracket-Kompaktmodus:** Toggle „100 % / Kompakt" im BracketTree — kleinere Nodes statt CSS-Transform (Connector-Vermessung bleibt korrekt), ganzer Baum ohne Scrollen.
- **Kür · Kalender-Export:** `lib/ics.ts → buildIcs()` + Button im Spielplan — exportiert die **gefilterten** Spiele als .ics (UTC-Zeiten, Stadion als Ort, KO-Platzhalter als Klartext), Save-Dialog via IPC `ics:export`.
- **App.tsx umgebaut:** RUBRIKEN + SCREENS-Map, Heute bekommt `goTo`; **Achtung: Sidebar-Indizes im Screenshot-Hook haben sich verschoben** (0 = Heute), Hook angepasst; neue Shots 15 (Heute mit aufgeklapptem Vergleich) + 16 (Baum kompakt).
- **Verifikation:** Typecheck grün, test:scoring 22 ✓, test:news 8 ✓, ICS gegen echten Spielplan geprüft (Katar–Schweiz 13.06. 21:00 MESZ, Escaping/CRLF ok), Shots 1/5/10/15/16 visuell geprüft (Rangliste-Totale unverändert 13/7/4 — Spiel-1-Vergleichs-Seed zählt nicht, da live).

### M5 ✅ — Release 1.0.0 (11.06. nachmittags)

- Version 0.1.0 → **1.0.0** (package.json), beide .dmg neu gebaut mit allem M4-Stand inkl. News-Feed und Stadion-Flip; alte 0.1.0-Artefakte aus `dist/` entfernt.
- Verifiziert: `codesign --verify` ok (adhoc), Info.plist 1.0.0, neue Features im app.asar nachgewiesen, **paketierte arm64-App durchläuft den kompletten Screenshot-Hook** (14/14, Fotos/Lagekarte/Fonts im Bundle ok).

### M4.4 ✅ — Panini-Spielerkarten (11.06. spätabends, Wunsch: „glossy und leicht beweglich")

- **Karten statt Text-Kacheln:** Die „Stars im Kader"-Kacheln sind jetzt echte Panini-Karten — weißer Sticker-Rand, Babyblau-Verlauf, Spielerfoto (oben angeschnitten), blauer Namensbalken (Oswald, uppercase), Rolle + Verein, Ecken-Chip „SUI · 1". Gewrappt in `HoloSticker`: **Regenbogen-Folie + Glare + 3D-Tilt beim Hover.**
- **Fotos:** 175/188 Spieler von Wikimedia Commons (8 parallele Recherche-Agenten, Lizenzen CC/PD verifiziert), via Special:FilePath geladen, auf 340 px/Q62 komprimiert (**6,2 MB gesamt**) → `assets/players/<slug>.jpg` + `credits.json`. **Slug-Algorithmus** `playerSlug()` in `lib/info.ts` — identisch im Download-Skript (lowercase, NFD-Diakritika weg, Rest `-`). Attribution per Mouseover auf dem Foto + Sammelzeile unter dem Grid.
- **Fallback ohne Foto** (13 Spieler, u. a. Mayele/Fajardo/Yıldız/Tounekti): große „26" + Initialen in Anton — Panini-Look bleibt.
- **Stichproben verifiziert:** Messi (White-House-Porträt 2026), Xhaka, Souttar — korrekte Personen, gute Qualität nach Kompression; Screenshot 12 zeigt die vier Nati-Karten.
- Bei Dossier-Updates (neue Stars in teaminfo) fehlende Fotos einfach nachladen: Batch-JSON + `/tmp/wm26-fetch-players.sh`-Muster (siehe Git-/Session-Historie).

### M5.2 ✅ — Punktesystem-Erklärung in der Rangliste (12.06., v1.4.2)

- **Nutzer-Feedback:** „(BRIEFING §5)" stand als Entwickler-Jargon im UI — alle BRIEFING-Referenzen aus sichtbaren Texten entfernt (Rangliste, KO-Einstieg; Rest war nur Code-Kommentar).
- **Neu:** Button „ⓘ Wie zählen die Punkte?" unter dem Rangliste-Lead → Album-Karte „So zählen die Punkte" mit zwei Spalten: Basispunkte pro Spiel (Pills, Gold für exakt) und Durchtipp-Boni (Gold für Weltmeister), dazu Klartext-Notizen zur KO-Regel (pro Spielnummer, 90 Minuten) und zum Späteinstieg. **Werte kommen live aus der ScoringConfig** — falls je verstellt, stimmt die Erklärung weiter.
- Verifiziert per Screenshot 10 (Hook öffnet das Panel jetzt mit); als v1.4.2 veröffentlicht (Banner-Update).
- **v1.4.3:** Boni-Spalte auf Nutzer-Feedback geschärft — Prinzip in einem Satz (Schnittmenge dein Baum/echt, pfadunabhängig) plus Gold-Beispielbox „Spanien bis zum Titel = 1+2+3+4+10 = 20 Bonuspunkte" (Summe live aus der Config) und Hinweis, wann gutgeschrieben wird. Stapel-Semantik gegen scoring.ts verifiziert.
- **v1.4.5 — Rangliste transparent bei zwei Wertungen:** Jede Tabelle hat jetzt Titel + Ein-Satz-Erklärung (`boardhint`): „Hauptwertung — Durchtippen ab Spiel 1" (Juni-Tipp läuft weiter, Basis pro Spielnummer, Boni) und „KO-Einstieg ab 1/16 — …" (alle tippen die echten Paarungen frisch, eigene Wertung, parallel). Begriff „Späteinstieg" im UI durch „KO-Einstieg" ersetzt (konsistent mit der Sidebar); Punktesystem-Note entsprechend geschärft.
- **v1.4.4 — KO-Aufruf auf dem Heute-Screen:** Klärung mit Adrian: Hauptwertung läuft in der KO-Phase weiter (Basis teamunabhängig, Tipps bis Anstoß änderbar, Durchtipp-Boni belohnen die Glaskugel); der Späteinstieg „ab 1/16" ist die Zusatzkategorie, in der ALLE mit den echten Paarungen frisch tippen. Damit das niemand verpasst: **goldener Banner auf Heute**, sobald alle 16 Sechzehntelfinal-Paarungen real feststehen (ESPN-Teams oder Tabellen-Auflösung) und die eigene fromR32-Kategorie unvollständig ist — mit Fortschritt (n/16) und Sprung zum KO-Einstieg; verschwindet nach dem letzten R32-Anstoß. Shot-Seed um die Paarungen 75–88 erweitert (Banner in Shot 15 verifiziert).

### M5.1 ✅ — Update-Mechanismus via GitHub (11.06. nachts, BRIEFING §11.11 Option A umgesetzt)

- **Repo:** `github.com/Ceccaroni/wm26-tipp-updates` (öffentlich — nur Manifest + Spieldaten, keine Tipps). Inhalt: `manifest.json` (App-Version, Download-URL, Daten-Laufnummer), `data-release.json` (~220 KB: Anstoßzeiten aller 104 Spiele, alle Dossiers, Quoten, alle 48 Kader), GitHub-Releases mit den .dmg — zusätzlich stabil benannte Kopien, damit `…/releases/latest/download/WM26-Tipp-arm64.dmg` als **ewiger Direkt-Download-Link** immer die neueste Version liefert (Banner-Button nutzt ihn; x64 analog).
- **App-Seite:** `UpdateService` im Main (Poll beim Start + alle 6 h, Retry 30 min, persistiert `wm26-update.json`, Push `update:update`). **Daten-Releases werden beim App-Start vor dem ersten Render in-place angewendet** (`lib/override.ts`: Schedule-Patches mutieren SCHEDULE-Objekte → GROUP_MATCHES & Co. sehen sie automatisch; Dossier-/Quoten-/Kader-Maps via `applyInfoOverride`). **Neuere App-Version ⇒ Gold-Banner** unten rechts („Version X ist da" + Notes + Download-Button → Releases-Seite im Browser; „Später" merkt sich die Version). Shot-Modus: kein Banner/Override.
- **Publishing (einseitig von Adrians Mac):** `npm run publish:update -- --notes "…" [--dmg]` — baut das Daten-Release aus `resources/data` (Ist-Stand, idempotent), zählt `dataVersion` hoch, schreibt beides via `gh api` ins Repo; `--dmg` lädt die .dmg der aktuellen Version als GitHub-Release hoch. **Workflow künftig: Daten ändern → `publish:update` (alle bekommen es automatisch); Code ändern → Version bumpen, `npm run dist` + `dist:intel`, `publish:update --dmg` (alle sehen den Banner).**
- **Verifiziert end-to-end:** Manifest + Daten v1/v2 live auf GitHub; Testlauf mit App-Version 1.3.9: Manifest geholt ✓, Daten-Release geladen + persistiert ✓, Override vor Render angewendet ✓, Banner „Version 1.4.0 verfügbar" gerendert ✓. `cmpVersion` getestet (inkl. 1.10 > 1.4). Hinweis: raw.githubusercontent cached frisch angelegte Dateien kurz negativ (404 für ~1 Min) — Retry fängt das ab.
- **Release v1.4.0** mit beiden .dmg auf GitHub veröffentlicht; paketierte App durchläuft alle 17 Screenshot-Screens.

### M4.5 ✅ — Komplette Kader als drehbare Panini-Karten (11.06. nachts)

- **Kader-Daten:** Alle 48 WM-Kader (1248 Spieler) aus der Wikipedia-Seite „2026 FIFA World Cup squads" geparst — neues Skript `scripts/build-squads.mjs` → `resources/data/squads/<ID>.json` (Nº, Position TW/AB/MF/ST, Geburtsdatum, Länderspiele, Tore, Verein, Captain-Flag). Validiert: 48×26, alle mit Geburtsdatum, keine Wiki-Syntax-Reste.
- **UI:** Button „🃏 Kompletter Kader" unter dem Hero-Sticker → Unterseite mit Positionsgruppen (Torhüter/Verteidigung/Mittelfeld/Sturm). Jede Karte ist **drehbar** (Klick/Enter): vorne Panini-Sticker mit Holo+Tilt, hinten große Rückennummer + Fakten (Position, Alter mit Geburtsdatum, Länderspiele, Tore, Verein, Captain). `SquadPlayer`-Typ in shared/types, Loader `squad()` in lib/info.ts.
- **Update 12.06. (v1.4.1):** Nachhol-Runde mit 8 Agenten (direkte Commons-Suche) → **981/1248 Fotos (79 %)**; Rest hat nachweislich kein frei lizenziertes Einzelfoto (v. a. RSA/HAI/JOR-Heimliga — und kurioserweise 9 Nati-Spieler). Dazu: **gedrehte Kader-Karte vergrößert sich 1,5×** (Lesbarkeit) und **nur noch eine Karte gleichzeitig gedreht** (Klick auf andere dreht die aktive zurück; Flip-State liegt bei SquadPage). Veröffentlicht als erstes Banner-Update (Release v1.4.1, Daten v4).
- **Fotos (Erstlauf):** 931/1248 (75 %) automatisch über **Wikidata P18** → Commons geladen — neues Skript `scripts/fetch-squad-photos.mjs` (Namenssuche mit Fußballer-Filter, Lizenz/Autor aus Commons-Metadaten, seriell mit 250-ms-Pause + Retry wegen Wikimedia-Rate-Limit, skip-existing, 340 px/Q62, gesamt 34 MB). Rest: Initialen-Fallback. Achtung: automatische Auswahl — vereinzelt Action-Shots mit Gegenspielern (z. B. Jashari); Austausch einzelner Bilder = Datei unter `assets/players/<slug>.jpg` ersetzen.
- **Verifikation:** Stichproben (Afif, Mvogo, Elvedi korrekt), Screenshot 17 (Kader Schweiz mit umgedrehter Karte), Typecheck grün.

### M4.2 ✅ — Stadion-Karten drehbar (Foto + Lagekarte)

- **Flip:** Klick (oder Enter) dreht die StadiumCard als 3D-Panini-Sticker (0,65 s, ↻-Hover-Hinweis, `prefers-reduced-motion` = instant). Vorderseite unverändert (Dossier + Spiele), Rückseite: Stadion-Foto + Nordamerika-Lagekarte + Lizenz-Zeile.
- **Fotos:** 16 frei lizenzierte Wikimedia-Commons-Bilder (per 2 Recherche-Agenten; CC BY / CC BY-SA / Public Domain), via Special:FilePath geladen, auf 900 px skaliert (gesamt ~3,2 MB), gebündelt unter `src/renderer/src/assets/stadiums/` + `credits.json` (Autor/Lizenz erscheint auf der Rückseite). Stichproben visuell verifiziert (Azteca-Nachtaufnahme mit Banorte-Branding, BC Place mit Seilnetz-Dach). Fehlt ein Foto, zeigt die Rückseite nur die Karte.
- **Lagekarte:** `components/VenueMap.tsx` — eigene Offline-SVG-Karte (Natural Earth 110m, Public Domain; 10 KB Pfad), generiert über neues Skript `scripts/build-venuemap.mjs` (Download → USA/CAN/MEX → equirektangulär → Ausschnitt). Alle 16 Spielorte als blaue Punkte, der gewählte gold mit Halo + Stadt-Label. Dafür `stadiums.json` um `lat`/`lon` erweitert (`Stadium`-Typ angepasst).
- **Verifikation:** Typecheck grün, Screenshot-Hook um Shot 14 (umgedrehte Azteca-Karte) erweitert und visuell geprüft.

### M4.1 v2 ✅ — Team-News intern lesbar (Feedback 11.06.: „keine externen Tabs, aufklappbar, dauernd aktualisierend")

- **Quellen statt Google News:** 7 verifizierte deutschsprachige RSS-/Atom-Feeds MIT Klartext (per 2 Recherche-Agenten geprüft: Textlänge, Aktualität, Format): **Blick WM** (de-CH, dedizierter WM-Feed!), **Watson Fussball** (Atom, ~680 Zeichen), **NZZ Sport**, **kicker WM**, **Sportschau WM 2026**, **t-online Sport** (Volltexte ~2700 Zeichen), **sport.de Fussball**. Liste in `shared/news.ts → FEEDS`. Hinweis: Blick blockt Browser-UAs (403) — unser eigener UA funktioniert (live verifiziert).
- **Architektur jetzt wie Results:** `NewsService` **pollt alle 15 Min** (Retry 3 Min), aggregiert, **persistiert atomar** (`wm26-news.json` → Offline-Start mit letztem Stand) und **pusht** `news:update` an den Renderer (Store: `news`/`newsFetchedAt`, `refreshNews()`).
- **Pure Logik** (`shared/news.ts`, testbar): generischer RSS-2.0/Atom-Parser (`content:encoded` ?? Atom-`content` ?? `description` ?? `summary`), `stripHtml` (CDATA, doppelte Entities, kein Leerraum vor Satzzeichen), **Team-Zuordnung per Namens-Stamm-Matching** (`ALIASES`, 48 Teams — „Schweizer"→SUI, aber Irankunda≠IRN), Dedupe nach Titel, 72-h-Alterslimit, max. 250 Items, Texte auf 1200 Zeichen gekappt (Wortgrenze).
- **UI:** „Aktuelle Meldungen" auf der Team-Seite als **Akkordeon — Klick klappt den Meldungstext IN der App auf** (keine Links, keine externen Tabs), Quelle + MESZ-Zeit, ↻-Button, Fußzeile „Aktualisiert sich automatisch alle 15 Min · Stand HH:MM".
- **Verifikation:** `test:news` neu geschrieben (14 Assertions: RSS/Atom, CDATA/HTML/Entities, content-Priorität, Team-Matching inkl. Kollisionsfällen, Dedupe/Alterslimit) grün; **Live-Lauf aller 7 Feeds** mit App-UA: 250 Meldungen, Team-Abdeckung u. a. SUI 33/GER 37/CUW 16, Stichproben gelesen (Blick-Nati-Meldungen). Typecheck grün, Screenshot 12 mit aufgeklappter Meldung geprüft.

### M4 ✅ — Album-Glanz

- **Kuratierte Daten-Releases** (BRIEFING §3.6/§3.7/§6, per 9 paralleler Web-Recherche-Agenten erstellt, alle Inhalte Stand 11.06.2026, Quellen kreuzgeprüft):
  - `resources/data/teaminfo/<ID>.json` (48): FIFA-Rang (April 2026), WM-Bilanz, Trainer (inkl. der vielen Last-Minute-Wechsel: KSA/GHA/TUN/MAR …), 3–4 Stars mit Verein Juni 2026, Form (März-Fenster + Juni-Generalproben mit Resultaten), Quali-Bilanz, 2–4 Storylines.
  - `resources/data/stadiuminfo/<id>.json` (16): Blurb + 3–4 Kurzfakten (Naturrasen-Umbauten der NFL-Stadien, Azteca-Sanierung, Dach/Klima, WM-Kapazitäten).
  - `resources/data/odds.json`: Quoten-Snapshot Turniersieg, alle 48 Teams, Dezimalquoten (BetMGM, Stand 10.06.2026, via DraftKings gegengeprüft).
  - Loader `lib/info.ts` (`import.meta.glob` über die Dossier-Ordner; fehlende Datei ⇒ UI zeigt Grundzustand). Typen `TeamInfo`/`StadiumInfo`/`OddsSnapshot` in `shared/types.ts`. Validierung beim Einpflegen: Team-/Stadion-IDs, Pflichtfelder, keine HTML-Escapes.
  - Hinweis Datenpflege: Dossier-Fakten (z. B. WM-Kapazität Azteca 87'523) können von den Stammdaten (`stadiums.json`: offizielle FIFA-Zahlen) abweichen — bewusst belassen, Stand-Datum steht dabei.
- **Teams-Rubrik = Galerie → Detailseite:** Album-Seite mit Panini-2026-Hero-Sticker (Babyblau, große „26" hinter der Flagge, blauer Datenbalken, Sticker-Nº, weißer Rand) als HoloSticker; Badges (Gruppe, FIFA-Rang), Trainer, Form/Quali/Storylines, Stars-Grid, Team-Spielplan (Gruppenspiele immer, KO-Spiele sobald ESPN das Team führt; mit Stadion) und Gold-Quotenbanner mit Quelle + Datumsstempel; Dossier-Stand sichtbar.
- **Stadien-Rubrik:** 16 StadiumCards (Papier-Look) nach Land gruppiert (MEX → CAN → USA), je Kapazität (de-CH-Format), TZ-Differenz zu MESZ, Dossier-Blurb + Fakten-Chips mit Stand-Datum, alle Spiele des Stadions, Highlight-Badge („Eröffnungsspiel", „Finale" mit Goldrand, „Halbfinale", …).
- **`components/MiniMatchRow.tsx`** (geteilt Stadien/Teams): kompakte Spielzeile mit echten Teams via ESPN-Results, End-/Live-Stand in der Mitte (rot bei live), optional Venue.
- **Holo-/Glossy-Effekte:** `styles/foil.css` + `components/HoloSticker.tsx` — pointer-getriebener Foil-Layer (`color-dodge`), Glare und 3D-Tilt (max. 8°) nach research/design.md §3; Gold-Variante nur als Belohnung (Weltmeister-Banner in „Meine Tipps" + KO-Einstieg). `prefers-reduced-motion` deaktiviert Folie + Tilt komplett.
- **Bracket-Connector-Linien:** SVG-Overlay in `BracketTree` — Kanten aus den `W<n>`-Slots des Spielplans abgeleitet, Positionen per `getBoundingClientRect` + ResizeObserver vermessen (H-V-H-Ellbogen), Kante wird gold, sobald der Baum den Weiterkommer kennt (gleiche Semantik wie der goldene Team-Pfad). Verlierer-Kanten (Platz 3) bewusst weggelassen.
- **Badge-Politur:** Auf hellen Albumseiten kräftige Tinten-Badges statt Pastell (`.album`/`.papercard`-scoped).
- **Verifikation:** Typecheck grün, `npm run test:scoring` grün, Screenshot-Hook um Shots 12 (Team-Detail Schweiz) + 13 (Stadien) erweitert; visuell geprüft inkl. Zoom (Hero-Sticker, Badge-Kontrast, Connector-Linien & Ellbogen im Live-Baum).
- **Bewusst offen (Nice-to-have):** Bracket-Zoom/Minimap, Team-Galerie-Sticker mit Voll-Holo (haben Shine-Sweep), Stadion-Fotos.

### M5 ✅ (lokal) — Release-Build

- **Fonts gebündelt:** 18 woff2 (Anton 400, Oswald/Inter 400–700, latin + latin-ext, ~730 KB) in `src/renderer/src/assets/fonts/`, eingebunden über `styles/fonts.css` — **kein Google-CDN mehr, App läuft komplett offline** (Erststart ohne Internet ok). CSP in `index.html` minimalisiert (`'self'` + inline-styles; ESPN/fixturedownload raus — Fetches laufen ohnehin im Main-Prozess).
- **Builds:** `npm run dist` / `dist:intel` → `dist/WM26 Tipp-0.1.0-arm64.dmg` (113 MB) + `-x64.dmg` (120 MB), ad-hoc signiert (electron-builder.yml war vorbereitet).
- **Verifiziert:** `codesign --verify` ok (`flags=0x2(adhoc)`), DMG mountet mit Drag-Install-Layout, **paketierte App durchläuft den kompletten Screenshot-Hook** (alle 11 Screens, Fonts/Stammdaten/Store im Bundle funktionieren), x64-Build läuft via Rosetta. README-Empfänger-Anleitung (macOS-15-Gatekeeper-Flow) war aktuell.
- **App-Icon ✅:** Panini-Sticker-Icon (weißer Rand, ⚽, Gold-Foil-Band „WM26" in Anton, Flutlicht-Squircle nach Apple-Raster). Pipeline: `npm run icon` → `scripts/make-icon.mjs` rendert `build/icon.html` mit Chromium (1024 px, Alpha) und baut via sips/iconutil `build/icon.icns`; electron-builder zieht es automatisch. Beide .dmg mit Icon neu gebaut + Signatur erneut verifiziert.
- **Noch offen für „M5 fertig":** (a) Gatekeeper-Flow auf einem **fremden** Mac real durchspielen (Quarantäne-Attribut setzt erst der Download/AirDrop), (b) Versionsnummer vor Verteilung ggf. auf 1.0.0.

### M3.4 ✅ — KO-Späteinstieg

- **`lib/lateEntry.ts`:** `LATE_ENTRIES` (fromR32/16/QF/SF mit Label, erster Spielnummer und Bonus-Runden) + `resolveLateBracket()` — Team-Auflösung je KO-Spiel: echte Teams (ESPN, auch vor Anpfiff) → echte Endstand-Auflösung → eigene Tipp-Kette → Platzhalter. Tabellen/Dritte kommen aus der echten Welt.
- **Wertungsregel:** Die Einstiegsrunde gibt keine Teilnehmer-Boni (ihr Feld ist real vorgegeben) — `computeBreakdown` hat dafür den Parameter `bonusRounds`; Weiterkommer-Boni gelten ab der Einstiegsrunde. Basis/Boni sonst identisch zur Hauptwertung.
- **KO-Einstieg-Screen:** Kategorie-Chips mit Fortschritt, Album-Seite mit Runden-Abschnitten, `TipRow` mit echten Qualifikanten (vorbefüllt, sobald ESPN sie kennt — auch vor Anpfiff), Platzhalter-Sticker + gesperrte Stepper für Unbekanntes, Hinweis-Banner bis die Einstiegsrunde feststeht, Weltmeister-Banner je Kategorie.
- **`TipRow` extrahiert** nach `components/TipRow.tsx` mit `entry`-Prop (Wertungskategorie); MeineTipps nutzt dieselbe Komponente.
- **Rangliste:** je Späteinstiegs-Kategorie eine eigene Sektion (erscheint erst, wenn jemand dort tippt), gleiche Zeilen/Aufschlüsselung (ohne Gruppen-Spieltage), FLIP über alle Sektionen.
- **Verifikation:** `npm run test:scoring` Fall 5 (Späteinstieg ab 1/8: 22 Assertions gesamt, grün), Typecheck grün, Screenshots 10/11: Rangliste-Sektion (5 P = 4 exakt + 1 Weiterkommer, Handrechnung bestätigt) und KO-Einstieg mit GER–SUI (beendet) + FRA–MAR (vorangekündigt, „Wer kommt weiter?"-Picker).

### M3.3 ✅ — Punkteberechnung komplett + Rangliste

- **`lib/scoring.ts` ausgebaut:** `baseKind()` (exakt/Differenz/Tendenz), `scoreMatch()` (API unverändert), `computeBreakdown()` = Gesamtwertung nach BRIEFING §5: Basis 4/3/2 + KO-Weiterkommer (+1, Vergleich getippter vs. echter Weiterkommer je Spielnummer) + Durchtipp-Boni (Schnittmenge getippte/echte Runden-Teilnehmer: R16 +1, VF +2, HF +3, Finalist +4, Weltmeister +10). Aufschlüsselung: Trefferzähler, Punkte je Gruppen-Spieltag und KO-Runde, `perMatch`.
- **Entscheid (BRIEFING §11.6, als Default gesetzt):** KO-Basispunkte teamunabhängig pro Spielnummer (kicktipp-üblich).
- **`lib/results.ts → buildRealWorld()`:** echte Weiterkommer + Runden-Teilnehmer aus `LiveResult.homeTeam/awayTeam` (ESPN, auch vor Anpfiff), Fallback eigene Auflösung der Endstände.
- **Rangliste (§3.4):** Hauptwertung `main` über alle Profile (eigene + importierte), Wettkampf-Ränge bei Punktgleichheit (Tiebreak exakte Treffer), Platz 1 Gold-Foil (nur bei > 0 P), aktives Profil markiert, Zeile aufklappbar (Spieltage/KO-Runden/Boni), **FLIP-animiert** bei Rangwechseln (respektiert `prefers-reduced-motion`). Späteinstiegs-Kategorien erscheinen mit M3.4.
- **Live-HUD konsistent:** „Punkte bisher" = `computeBreakdown().total` (mit Boni, identisch zur Rangliste); Punkte-Pills enthalten den Weiterkommer-Bonus (Foil weiter nur bei exaktem Basistreffer).
- **Neuer Test `npm run test:scoring`** (`scripts/test-scoring.ts`, esbuild + node): 18 Assertions über synthetische Volltreffer-/Falschtipper-/Teilstand-Szenarien — deckt implizit auch `tournament.ts`/`bracket.ts`-Ketten ab (Lücke aus M2 teilweise geschlossen). Lehrreicher Befund: auch ein komplett falsch liegender Tipper kann legitime Durchtipp-Boni holen (zufällig richtige Achtelfinalisten im Baum) — gewollt.
- **Verifikation:** Test grün, Typecheck grün, Screenshot 10 (Rangliste mit 3 Profilen) stimmt mit Handrechnung überein (13/7/4 P).

### M3.2 ✅ — Live-Rubrik

- **Drei Ansichten** (Chips wie in „Meine Tipps"): **Spiele** (Jetzt live / Heute / Ergebnisse nach Tagen, neueste zuerst), **Tabellen** (12 echte Gruppentabellen als Papier-Karten), **Baum** (echter Turnierbaum).
- **Wiederverwendung statt Neubau:** `lib/results.ts → resultsAsTips()` wandelt Endstände in Tipp-Form (`h/a/adv`) — echte Tabellen/Baum laufen durch dieselben `computeGroupTable()`/`resolveTipBracket()` wie die Tipp-Welt. Nur beendete Spiele zählen.
- **LiveRow:** Status-Block (Minute rot pulsierend / Halbzeit / Endstand / n.V. / x:y i.E. / Anstoßzeit), Sieger gold, eigener Tipp dezent + Punkte-Pill (`scoreMatch`): Gold-Foil = exakt (Belohnungs-Regel), gestrichelt = provisorisch während des Spiels, „kein Tipp" gedimmt.
- **GlassHUD** (sticky, blur): Live-Zähler mit Puls-Punkt (respektiert `prefers-reduced-motion`), „Punkte bisher" (Basis 4/3/2 — Boni folgen in M3.3), Stale-Anzeige „Stand 21:43" (amber ab 10 Min.), Refresh-Button (`results:refresh`).
- **KO-Paarungen:** `LiveResult` trägt jetzt `homeTeam`/`awayTeam` (FIFA-Trigramm) aus ESPN bzw. fixturedownload (feedName-Mapping) — auch **vor** Anpfiff. Anzeige-Priorität: ESPN-Teams → eigener echter Bracket-Resolve → Platzhalter. Damit sind echte KO-Ansetzungen unabhängig von Tiebreaks korrekt (Fairplay-Punkte können wir nicht nachrechnen) — auch Grundlage für M3.4 (KO-Einstieg vorbefüllen).
- **BracketTree:** neue Prop `championCaption` („Weltmeister" statt „Dein Weltmeister" im echten Baum).
- **Screenshot-Hook erweitert:** Shot-Modus seedet jetzt deterministisch Profil + Tipps + alle Ergebnis-Zustände (live/HT/Endstand/n.V./i.E./ohne Tipp) und lässt den Poller aus; neue Shots 7–9 (Live-Spiele/-Tabellen/-Baum).
- **Verifikation:** Typecheck grün; Screenshots visuell geprüft (HUD, Pills, MESZ-Tagesgruppierung — Spiel #7 22:00Z landet korrekt am Folgetag, Tabellen zählen Live-Spiele nicht, ⚖-Marker bei Gleichstand); Parser-Regressionstest WM-Finale 2022 inkl. Team-Extraktion; Echtlauf im Normalmodus ohne Fehler.

### M3.1 ✅ — Ergebnis-Polling

- **Architektur:** Fetch ausschließlich im Main-Prozess. `src/shared/results.ts` = pure Logik (Mapping, Parser, Spielfenster — von beiden TS-Projekten nutzbar, testbar), `src/main/results.ts` = `ResultsService` (Timer, Fetch, Persistenz, IPC-Push). Renderer fetcht nie.
- **Event-Mapping** (ESPN `event.id` → Spielnummer 1–104, einmal gemappt = persistiert): Kaskade Teampaar → exakte Anstoßzeit → gleicher UTC-Tag + nächste Zeit. Gegen den echten Feed verifiziert: **104/104 eindeutig**. KO-Anstöße sind nie gleichzeitig (geprüft), daher reicht dort Zeit-Matching.
- **Polling:** 90 s im Spielfenster (Anstoß −1 h bis +3,5 h, oder solange ein Spiel live gemeldet ist), sonst bis zum nächsten Fensterbeginn, max. 1 h. Fehler-Backoff 2^n (max. ×8). fixturedownload stündlich als Endstand-Fallback — füllt nur Spiele, wo ESPN nichts Belastbares hat (`MatchNumber` = unsere Spielnummer, kein Mapping nötig).
- **Persistenz:** `wm26-results.json` (eventMap + results + fetchedAt) atomar im userData → Offline-Start zeigt letzten Stand; `fetchedAt` für Stale-Anzeige („Stand 21:43").
- **IPC/Store:** `results:get` / `results:refresh` (manueller Button) / `results:update`-Push → Zustand-Store `results`, `resultsFetchedAt`, Hook `useResult(match)`.
- **Typen:** `LiveResult` um `winner: 'home'|'away'` erweitert (KO-Sieger auch nach Elfmeterschießen — wichtig für Punkte + echten Baum); neu `ResultsSnapshot`. `lib/resultsApi.ts` (Stub) gelöscht.
- **Verifikation:** Parser gegen echte historische ESPN-Daten: WM-Finale 2022 (3:3 n.V., 4:2 i.E., `aet`+`pens`+`winner` ✓, Heim/Auswärts-Spiegelung ✓), WM-HF 2018 (2:1 n.V. ohne Elfmeter ✓). Kaltstart (Voll-Mapping), Warmstart (Cache, Dedupe, schmales Abruf-Fenster), Renderer-Empfang (104 Results im Store), Typecheck grün. StrictMode-Doppel-Init im Store synchron abgefangen.
- **Befund am Rande:** ESPN führt Spiel 29 (BRA–HAI) und 31 (TUR–PAR) **30/60 Min. früher** als schedule.json/fixturedownload — vermutlich Anstoß-Verschiebung, die im FIFA-Feed noch nicht durch ist. Mapping fängt das ab; vor dem 20.06. prüfen und ggf. `npm run data:schedule` laufen lassen, sobald fixturedownload nachzieht (sonst zeigt der Spielplan die falsche MESZ-Zeit).
- **Hinweis:** Status `scheduled` liefert bewusst keine Scores (ESPN sendet vor Anpfiff „0"–„0"). Erstes echtes Live-Spiel heute 21:00 MESZ — Fenster öffnet automatisch um 20:00.

---

## Archiv: Stand 11. Juni 2026, mittags

**Kurzfassung: M1 + M2 fertig und verifiziert. Komplettes Durchtippen bis zum Weltmeister funktioniert. Als Nächstes: M3 (Live, Punkte, Rangliste, KO-Einstieg).**

### M2 ✅ — Turnierlogik komplett

- **Gruppentabellen** live aus Tipps: `lib/tournament.ts → computeGroupTable()` mit offiziellen FIFA-Tiebreakern 2026 (direkter Vergleich der Punktgleichen vor Gesamt-TD; bewusst ohne rekursive Sub-Untergruppen-Regel, dokumentiert im Code). Gleichstände → deterministischer Fallback, im UI als ⚖ markiert.
- **Annexe C offiziell:** `resources/data/annexc.json` — alle 495 Kombinationen, per Agent aus dem FIFA-Regularien-PDF (S. 81 ff., pdftotext) extrahiert, validiert via `scripts/validate-annexc.mjs` (Bijektion, Kandidatenmengen, 20 PDF-Stichproben). Backtracking-Fallback in `thirdPlace.ts` bleibt als Sicherheitsnetz (`official: false` → UI-Hinweis).
- **Bracket-Auflösung:** `lib/bracket.ts → resolveTipBracket()` — Tabellen → Dritten-Ranking → Annexe-C-Zuordnung → KO-Kette (W/L-Slots) bis `champion`. Spiele 73–104 aufsteigend aufgelöst, memoisiert via useMemo.
- **KO-Durchtippen:** „Meine Tipps" hat jetzt Stage-Chips A–L + 1/16, 1/8, VF, HF, Finale + Baum. KO-TipRows mit aufgelösten Teams, „Wer kommt weiter?"/„Wer gewinnt?"-Picker bei Unentschieden (`tip.adv`), Steppers gesperrt solange Teams unaufgelöst. Dritten-Leiste (8 von 12 gold) auf der R32-Seite, Weltmeister-Foil-Banner auf der Finale-Seite.
- **Turnierbaum:** `components/BracketTree.tsx` — gespiegelt, Finale in der Mitte (9 Spalten), Sieger-Pfad gold, Champion-Sticker zentral. Display-only (Tippen in den Stage-Listen); Connector-Linien/Zoom = M4-Politur.
- **Verifikation:** End-to-End per Demo-Store (alle 104 Spiele deterministisch getippt, Seed-Snippet siehe Git-Historie/Session) + Screenshot-Hook: Tabellen ✓, Dritten-Strip ✓, R32-Teams konsistent mit Gruppen ✓, Finale/Platz-3-Teilnehmer = Sieger/Verlierer der Halbfinals ✓, Champion ✓. Typecheck + Build grün.

---

## Archiv: Stand 11. Juni 2026, vormittags

### Was funktioniert (M1 ✅)

- **Onboarding & Profile:** Profil anlegen (Name + Farbe), mehrere Profile, Wechsel, aktives Profil in der Sidebar.
- **Spielplan:** alle 104 Spiele nach Tagen (MESZ), Filter Runde/Gruppe/Team, „Heute"-Sprung, MatchCards mit Flaggen-Stickern; KO-Platzhalter als blaue Gruppen-/Nummern-Sticker (`slotInfo()`).
- **Meine Tipps:** Gruppenphase (72 Spiele) mit ScoreStepper, Auto-Save bei jedem Klick (optimistisch im Renderer, atomar im Main), Sperre ab Anstoß („eingeklebt"/„verpasst"), Gruppen-Chips mit Fortschritt.
- **Teams:** Galerie aller 48 Teams mit Flaggen (vorgezogen aus M4 wegen Nutzerfeedback „nicht alle Flaggen drin"). Detailseiten fehlen noch (M4).
- **Export/Import:** `.wm26tipp` (JSON, formatVersion 1) über Profile-Rubrik; Merge-Regel: neuerer Export gewinnt, eigene Profile geschützt.
- **Verifikation:** Typecheck grün, Build grün, UI per Screenshot-Hook geprüft (`WM26_SHOT_DIR`).

### Bewusste Lücken / Platzhalter-Screens

- **Live, Rangliste, KO-Einstieg, Stadien:** nur Platzhaltertexte (kommen M3/M4).
- `lib/resultsApi.ts`: Signatur vorhanden, wirft `TODO M3`.
- Fonts laden von Google CDN → App braucht beim ersten Start Internet; für Release ggf. Fonts bündeln (M5).
- `.dmg`-Build (`npm run dist`) noch nie ausgeführt/getestet — Teil von M5.
- Punkteberechnung (`lib/scoring.ts → scoreMatch`) implementiert, aber noch ungenutzt und ungetestet (M3).
- Kein automatisierter Test für `tournament.ts`/`bracket.ts` — Verifikation bisher End-to-End via Demo-Store; Unit-Tests (z. B. Tiebreaker-Kanten: 3 punktgleiche Teams) wären sinnvoll (M3/M5).
- Änderung eines Gruppentipps NACH gesetzten KO-Tipps lässt die KO-Tipps an ihren Spielnummern stehen (Teams können wechseln) — Standard-Durchtipp-Verhalten, kein UI-Hinweis.

### Nächste Schritte

**Alle Meilensteine M1–M5 ✅, Version 1.1.0 verteilt (11.06. abends, arm64 an die Mittipper).** Es bleibt:

1. **Letzte manuelle DMG-Runde:** `WM26 Tipp-1.4.0-arm64.dmg` an alle verteilen (per Chat oder Link auf github.com/Ceccaroni/wm26-tipp-updates/releases) — ab dieser Version aktualisieren sich Daten automatisch und neue Versionen melden sich per Banner. Die verteilte 1.1.0 kann das noch nicht!
2. **Verteilung nachhalten:** Rückmeldung der Empfänger zum Gatekeeper-Flow abwarten; danach `.wm26tipp`-Austausch starten, damit die Rangliste lebt.
3. **Erledigt 12.06. (M6):** Spiel 29/31 gegen offizielle Quellen verifiziert und in schedule.json gepatcht (verteilt mit Daten v11). **Achtung:** `npm run data:schedule` erst wieder laufen lassen, wenn fixturedownload die neuen Zeiten (00:30Z/03:00Z) führt — sonst wird der Patch überschrieben.
4. **Datenpflege während des Turniers:** `teaminfo`/`stadiuminfo`/`odds.json` bei Bedarf per neuem Daten-Release auffrischen (BRIEFING §6) — Stand-Datum wird im UI angezeigt; News/Ergebnisse aktualisieren sich von selbst.

### Entscheidungs-Log (Details: BRIEFING.md §11)

- 11.06.: Punktesystem 4/3/2 + Boni bestätigt · Späteinstieg = eigene Kategorie pro Runde · beide .dmg-Architekturen bauen · electron-store → eigener Atomic-Store · Teams-Galerie vorgezogen.

### Wo liegt was

- Echte Tippdaten: `~/Library/Application Support/WM26 Tipp/wm26-store.json` + `wm26-results.json` (Electron nimmt `productName`; Dev und paketierte App teilen den Pfad — Tipps überleben den Umstieg auf die .dmg. Screenshot-Hook nutzt isoliertes userData unter `$WM26_SHOT_DIR/userData`).
- Stammdaten: `resources/data/` (teams, stadiums, schedule, bracket + `source/`-Rohfeed).
- Port-Hinweis: 5173 gehört einem fremden Projekt (ChemieSandbox-`npx vite`); unsere App weicht automatisch auf 5174+ aus.
